basePath: /api/v2/cmdb
consumes:
- application/json
host: 172.16.177.65
info:
  contact:
    email: fosguiqateam@fortinet.com
    name: FortiOS Web Application Team
    url: http://modskristensen.net
  description: REST API for configuring FortiOS objects and settings
  termsOfService: http://fortinet.com/terms/
  title: FortiOS CMDB
  version: v2
paths:
  /web-proxy/debug-url:
    get:
      parameters:
      - description: Enable to include datasource information for each linked object.
        in: query
        name: datasource
        required: false
        type: boolean
      - description: Starting entry index.
        in: query
        name: start
        required: false
        type: integer
      - description: Maximum number of entries to return.
        in: query
        name: count
        required: false
        type: integer
      - description: Enable to include meta information about each object (type id,
          references, etc).
        in: query
        name: with_meta
        required: false
        type: boolean
      - description: Enable to include a checksum of each object's contents.
        in: query
        name: with_contents_hash
        required: false
        type: boolean
      - description: Enable to call CLI skip operator to hide skipped properties.
        in: query
        name: skip
        required: false
        type: boolean
      - collectionFormat: pipes
        description: List of property names to include in results, separated by |
          (i.e. policyid|srcintf).
        in: query
        items:
          type: string
        name: format
        required: false
        type: array
      - collectionFormat: multi
        description: "Filtering multiple key/value pairs\nOperator     |   Description\n\
          ==     |   Case insensitive match with pattern.\n!=     |    Does not match\
          \ with pattern (case insensitive).\n=@     |    Pattern found in object\
          \ value (case insensitive).\n!@     |    \uFEFFPattern not\uFEFF found in\
          \ object value (case insensitive).\n<=     |    Value must be less than\
          \ or equal to \uFEFFpattern\uFEFF.\n<     |    Value must be less than pattern\uFEFF\
          .\n.>=    |    Value must be greater than or equal to \uFEFFpattern\uFEFF\
          .\n.>     |    Value must be greater than \uFEFFpattern\uFEFF.\nLogical\
          \ OR    |    Separate filters using commas ','\nLogical AND    |    Filter\
          \ strings can be combined to create logical AND queries by including multiple\
          \ filters in the request.\nCombining AND and OR    |    You can combine\
          \ AND and OR filters together to create more complex filters.\n"
        in: query
        items:
          type: string
        name: filter
        required: false
        type: array
      - description: If present, objects will be filtered on property with this name.
        in: query
        name: key
        required: false
        type: string
      - description: If present, objects will be filtered on property with this value.
        in: query
        name: pattern
        required: false
        type: string
      - description: Scope [global|vdom|both*]
        in: query
        name: scope
        required: false
        type: string
      - description: Exclude properties/objects with default value
        in: query
        name: exclude-default-values
        required: false
        type: boolean
      - description: 'default: Return the CLI default values for entire CLI tree.

          meta: Return meta data for a specific object, table, or the entire CLI tree.

          schema: Return schema for entire CLI tree.

          '
        enum:
        - default
        - schema
        in: query
        name: action
        required: false
        type: string
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      responses:
        '200':
          description: 'OK: Request returns successful'
          schema:
            properties:
              exact:
                description: "Enable/disable matching the exact path.   \nenable:Enable\
                  \ matching the exact path.   \ndisable:Disable matching the exact\
                  \ path."
                enum:
                - enable
                - disable
                type: string
              name:
                description: Debug URL name.
                maxLength: 63
                type: string
              status:
                description: "Enable/disable this URL exemption.   \nenable:Enable\
                  \ this URL exemption.   \ndisable:Disable this URL exemption."
                enum:
                - enable
                - disable
                type: string
              url-pattern:
                description: URL exemption pattern.
                maxLength: 511
                type: string
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Select all entries in a CLI table.\n Access Group: netgrp"
      tags:
      - web-proxy/debug-url
    post:
      parameters:
      - description: Possible parameters to go in the body for the request
        in: body
        name: body
        required: true
        schema:
          properties:
            exact:
              description: "Enable/disable matching the exact path.   \nenable:Enable\
                \ matching the exact path.   \ndisable:Disable matching the exact\
                \ path."
              enum:
              - enable
              - disable
              type: string
            name:
              description: Debug URL name.
              maxLength: 63
              type: string
            status:
              description: "Enable/disable this URL exemption.   \nenable:Enable this\
                \ URL exemption.   \ndisable:Disable this URL exemption."
              enum:
              - enable
              - disable
              type: string
            url-pattern:
              description: URL exemption pattern.
              maxLength: 511
              type: string
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      - description: "If supported, an action can be specified.\n_clone_: Clone this\
          \ specific resource.\nWhen *action=clone* is set, the extra parameters *nkey*\
          \ must be provided.\n__*Note:*__ If this parameter is provided when not\
          \ supported, the action will be ignored and an \u201Cinvalid request\u201D\
          \ error will be returned.\n"
        enum:
        - clone
        in: query
        name: action
        required: false
        type: string
      - description: ' If *action=clone*, use *nkey* to specify the ID for the new
          resource to be created.

          For example, to clone `address1` to `address1_clone`, use:

          __action=clone&nkey=address1_clone__

          __*Note:*__ This parameter can only be used when the *action* parameter
          is set to *clone*.

          '
        in: query
        name: nkey
        required: false
        type: string
      responses:
        '200':
          description: 'OK: Request returns successful'
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Create object(s) in this table.\n Access Group: netgrp"
      tags:
      - web-proxy/debug-url
  /web-proxy/debug-url/{name}:
    delete:
      parameters:
      - description: mkey
        in: path
        name: name
        required: true
        type: string
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      responses:
        '200':
          description: 'OK: Request returns successful'
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Delete this specific resource.\n Access Group: netgrp"
      tags:
      - web-proxy/debug-url
    get:
      parameters:
      - description: Enable to include datasource information for each linked object.
        in: query
        name: datasource
        required: false
        type: boolean
      - description: Enable to include meta information about each object (type id,
          references, etc).
        in: query
        name: with_meta
        required: false
        type: boolean
      - description: Enable to call CLI skip operator to hide skipped properties.
        in: query
        name: skip
        required: false
        type: boolean
      - collectionFormat: pipes
        description: List of property names to include in results, separated by |
          (i.e. policyid|srcintf).
        in: query
        items:
          type: string
        name: format
        required: false
        type: array
      - description: 'default: Return the CLI default values for this object type.

          schema: Return the CLI schema for this object type.

          revision: Return the CMDB revision for this object type.

          transaction-list: List all configuration transaction(s).

          '
        enum:
        - default
        - schema
        - revision
        in: query
        name: action
        required: false
        type: string
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      - description: mkey
        in: path
        name: name
        required: true
        type: string
      responses:
        '200':
          description: 'OK: Request returns successful'
          schema:
            properties:
              exact:
                description: "Enable/disable matching the exact path.   \nenable:Enable\
                  \ matching the exact path.   \ndisable:Disable matching the exact\
                  \ path."
                enum:
                - enable
                - disable
                type: string
              name:
                description: Debug URL name.
                maxLength: 63
                type: string
              status:
                description: "Enable/disable this URL exemption.   \nenable:Enable\
                  \ this URL exemption.   \ndisable:Disable this URL exemption."
                enum:
                - enable
                - disable
                type: string
              url-pattern:
                description: URL exemption pattern.
                maxLength: 511
                type: string
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Select a specific entry from a CLI table.\n Access Group: netgrp"
      tags:
      - web-proxy/debug-url
    put:
      parameters:
      - description: Possible parameters to go in the body for the request
        in: body
        name: body
        required: true
        schema:
          properties:
            exact:
              description: "Enable/disable matching the exact path.   \nenable:Enable\
                \ matching the exact path.   \ndisable:Disable matching the exact\
                \ path."
              enum:
              - enable
              - disable
              type: string
            name:
              description: Debug URL name.
              maxLength: 63
              type: string
            status:
              description: "Enable/disable this URL exemption.   \nenable:Enable this\
                \ URL exemption.   \ndisable:Disable this URL exemption."
              enum:
              - enable
              - disable
              type: string
            url-pattern:
              description: URL exemption pattern.
              maxLength: 511
              type: string
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      - description: "If supported, an action can be specified.\n_move_: Move this\
          \ specific resource.\nWhen *action=move* is set, one of the extra parameters\
          \ (*before*, *after*) must be provided.\n__*Note:*__ If this parameter is\
          \ provided when not supported, the action will be ignored and an \u201C\
          invalid request\u201D error will be returned.\n"
        enum:
        - move
        in: query
        name: action
        required: false
        type: string
      - description: 'If *action=move*, use *before* to specify the ID of the resource
          that this resource will be moved before.

          For example, to move `object 1` to before `object 2`, use:

          __action=move&before=2__

          __*Note:*__ This parameter can only be used when the *action* parameter
          is set to *move*.

          '
        in: query
        name: before
        required: false
        type: string
      - description: 'If *action=move*, use *after* to specify the ID of the resource
          that this resource will be moved after.

          For example, to move `object 1` to after `object 3`, use:

          __action=move&after=3__

          __*Note:*__ This parameter can only be used when the *action* parameter
          is set to *move*.

          '
        in: query
        name: after
        required: false
        type: string
      - description: mkey
        in: path
        name: name
        required: true
        type: string
      responses:
        '200':
          description: 'OK: Request returns successful'
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Update this specific resource.\n Access Group: netgrp"
      tags:
      - web-proxy/debug-url
  /web-proxy/explicit:
    get:
      parameters:
      - description: Enable to include datasource information for each linked object.
        in: query
        name: datasource
        required: false
        type: boolean
      - description: Starting entry index.
        in: query
        name: start
        required: false
        type: integer
      - description: Maximum number of entries to return.
        in: query
        name: count
        required: false
        type: integer
      - description: Enable to include meta information about each object (type id,
          references, etc).
        in: query
        name: with_meta
        required: false
        type: boolean
      - description: Enable to include a checksum of each object's contents.
        in: query
        name: with_contents_hash
        required: false
        type: boolean
      - description: Enable to call CLI skip operator to hide skipped properties.
        in: query
        name: skip
        required: false
        type: boolean
      - collectionFormat: pipes
        description: List of property names to include in results, separated by |
          (i.e. policyid|srcintf).
        in: query
        items:
          type: string
        name: format
        required: false
        type: array
      - collectionFormat: multi
        description: "Filtering multiple key/value pairs\nOperator     |   Description\n\
          ==     |   Case insensitive match with pattern.\n!=     |    Does not match\
          \ with pattern (case insensitive).\n=@     |    Pattern found in object\
          \ value (case insensitive).\n!@     |    \uFEFFPattern not\uFEFF found in\
          \ object value (case insensitive).\n<=     |    Value must be less than\
          \ or equal to \uFEFFpattern\uFEFF.\n<     |    Value must be less than pattern\uFEFF\
          .\n.>=    |    Value must be greater than or equal to \uFEFFpattern\uFEFF\
          .\n.>     |    Value must be greater than \uFEFFpattern\uFEFF.\nLogical\
          \ OR    |    Separate filters using commas ','\nLogical AND    |    Filter\
          \ strings can be combined to create logical AND queries by including multiple\
          \ filters in the request.\nCombining AND and OR    |    You can combine\
          \ AND and OR filters together to create more complex filters.\n"
        in: query
        items:
          type: string
        name: filter
        required: false
        type: array
      - description: If present, objects will be filtered on property with this name.
        in: query
        name: key
        required: false
        type: string
      - description: If present, objects will be filtered on property with this value.
        in: query
        name: pattern
        required: false
        type: string
      - description: Scope [global|vdom|both*]
        in: query
        name: scope
        required: false
        type: string
      - description: Exclude properties/objects with default value
        in: query
        name: exclude-default-values
        required: false
        type: boolean
      - description: 'default: Return the CLI default values for entire CLI tree.

          meta: Return meta data for a specific object, table, or the entire CLI tree.

          schema: Return schema for entire CLI tree.

          '
        enum:
        - default
        - schema
        in: query
        name: action
        required: false
        type: string
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      responses:
        '200':
          description: 'OK: Request returns successful'
          schema:
            properties:
              ftp-incoming-port:
                description: Accept incoming FTP-over-HTTP requests on one or more
                  ports (0 - 65535, default = 0; use the same as HTTP).
                type: string
              ftp-over-http:
                description: "Enable to proxy FTP-over-HTTP sessions sent from a web\
                  \ browser.   \nenable:Enable FTP-over-HTTP sessions.   \ndisable:Disable\
                  \ FTP-over-HTTP sessions."
                enum:
                - enable
                - disable
                type: string
              http-incoming-port:
                description: Accept incoming HTTP requests on one or more ports (0
                  - 65535, default = 8080).
                type: string
              https-incoming-port:
                description: Accept incoming HTTPS requests on one or more ports (0
                  - 65535, default = 0, use the same as HTTP).
                type: string
              https-replacement-message:
                description: "Enable/disable sending the client a replacement message\
                  \ for HTTPS requests.   \nenable:Display a replacement message for\
                  \ HTTPS requests.   \ndisable:Do not display a replacement message\
                  \ for HTTPS requests."
                enum:
                - enable
                - disable
                type: string
              incoming-ip:
                description: Restrict the explicit HTTP proxy to only accept sessions
                  from this IP address. An interface must have this IP address.
                format: ipv4
                type: string
              incoming-ip6:
                description: Restrict the explicit web proxy to only accept sessions
                  from this IPv6 address. An interface must have this IPv6 address.
                format: ipv6
                type: string
              ipv6-status:
                description: "Enable/disable allowing an IPv6 web proxy destination\
                  \ in policies and all IPv6 related entries in this command.   \n\
                  enable:Enable allowing an IPv6 web proxy destination.   \ndisable:Disable\
                  \ allowing an IPv6 web proxy destination."
                enum:
                - enable
                - disable
                type: string
              message-upon-server-error:
                description: "Enable/disable displaying a replacement message when\
                  \ a server error is detected.   \nenable:Display a replacement message\
                  \ when a server error is detected.   \ndisable:Do not display a\
                  \ replacement message when a server error is detected."
                enum:
                - enable
                - disable
                type: string
              outgoing-ip:
                description: Outgoing HTTP requests will have this IP address as their
                  source address. An interface must have this IP address.
                format: ipv4
                type: string
              outgoing-ip6:
                description: Outgoing HTTP requests will leave this IPv6. Multiple
                  interfaces can be specified. Interfaces must have these IPv6 addresses.
                format: ipv6
                type: string
              pac-file-data:
                description: PAC file contents enclosed in quotes (maximum of 256K
                  bytes).
                type: string
              pac-file-name:
                description: Pac file name.
                maxLength: 63
                type: string
              pac-file-server-port:
                description: Port number that PAC traffic from client web browsers
                  uses to connect to the explicit web proxy (0 - 65535, default =
                  0; use the same as HTTP).
                type: string
              pac-file-server-status:
                description: "Enable/disable Proxy Auto-Configuration (PAC) for users\
                  \ of this explicit proxy profile.   \nenable:Enable Proxy Auto-Configuration\
                  \ (PAC).   \ndisable:Disable Proxy Auto-Configuration (PAC)."
                enum:
                - enable
                - disable
                type: string
              pac-file-url:
                description: PAC file access URL.
                type: string
              pac-policy:
                description: PAC policies.
                items:
                  properties:
                    comments:
                      description: Optional comments.
                      maxLength: 1023
                      type: string
                    dstaddr:
                      description: Destination address objects.
                      items:
                        properties:
                          name:
                            description: Address name.
                            maxLength: 79
                            type: string
                        type: object
                      maxItems: null
                      type: array
                    pac-file-data:
                      description: PAC file contents enclosed in quotes (maximum of
                        256K bytes).
                      type: string
                    pac-file-name:
                      description: Pac file name.
                      maxLength: 63
                      type: string
                    policyid:
                      description: Policy ID.
                      maximum: 100
                      minimum: 1
                      type: integer
                    srcaddr:
                      description: Source address objects.
                      items:
                        properties:
                          name:
                            description: Address name.
                            maxLength: 79
                            type: string
                        type: object
                      maxItems: null
                      type: array
                    srcaddr6:
                      description: Source address6 objects.
                      items:
                        properties:
                          name:
                            description: Address name.
                            maxLength: 79
                            type: string
                        type: object
                      maxItems: null
                      type: array
                    status:
                      description: "Enable/disable policy.   \nenable:Enable policy.\
                        \   \ndisable:Disable policy."
                      enum:
                      - enable
                      - disable
                      type: string
                  type: object
                maxItems: null
                type: array
              pref-dns-result:
                description: "Prefer resolving addresses using the configured IPv4\
                  \ or IPv6 DNS server (default = ipv4).   \nipv4:Prefer the IPv4\
                  \ DNS server.   \nipv6:Prefer the IPv6 DNS server."
                enum:
                - ipv4
                - ipv6
                type: string
              realm:
                description: Authentication realm used to identify the explicit web
                  proxy (maximum of 63 characters).
                maxLength: 63
                type: string
              sec-default-action:
                description: "Accept or deny explicit web proxy sessions when no web\
                  \ proxy firewall policy exists.   \naccept:Accept requests. All\
                  \ explicit web proxy traffic is accepted whether there is an explicit\
                  \ web proxy policy or not.   \ndeny:Deny requests unless there is\
                  \ a matching explicit web proxy policy."
                enum:
                - accept
                - deny
                type: string
              socks:
                description: "Enable/disable the SOCKS proxy.   \nenable:Enable the\
                  \ SOCKS proxy.   \ndisable:Disable the SOCKS proxy."
                enum:
                - enable
                - disable
                type: string
              socks-incoming-port:
                description: Accept incoming SOCKS proxy requests on one or more ports
                  (0 - 65535, default = 0; use the same as HTTP).
                type: string
              ssl-algorithm:
                description: "Relative strength of encryption algorithms accepted\
                  \ in HTTPS deep scan: high, medium, or low.   \nhigh:High encrption.\
                  \ Allow only AES and ChaCha.   \nmedium:Medium encryption. Allow\
                  \ AES, ChaCha, 3DES, and RC4.   \nlow:Low encryption. Allow AES,\
                  \ ChaCha, 3DES, RC4, and DES."
                enum:
                - high
                - medium
                - low
                type: string
              status:
                description: "Enable/disable the explicit Web proxy for HTTP and HTTPS\
                  \ session.   \nenable:Enable the explicit web proxy.   \ndisable:Disable\
                  \ the explicit web proxy."
                enum:
                - enable
                - disable
                type: string
              strict-guest:
                description: "Enable/disable strict guest user checking by the explicit\
                  \ web proxy.   \nenable:Enable strict guest user checking.   \n\
                  disable:Disable strict guest user checking."
                enum:
                - enable
                - disable
                type: string
              trace-auth-no-rsp:
                description: "Enable/disable logging timed-out authentication requests.\
                  \   \nenable:Enable logging timed-out authentication requests. \
                  \  \ndisable:Disable logging timed-out authentication requests."
                enum:
                - enable
                - disable
                type: string
              unknown-http-version:
                description: "How to handle HTTP sessions that do not comply with\
                  \ HTTP 0.9, 1.0, or 1.1.   \nreject:Reject or tear down HTTP sessions\
                  \ that do not use HTTP 0.9, 1.0, or 1.1.   \ntunnel:Pass HTTP traffic\
                  \ that does not use HTTP 0.9, 1.0, or 1.1 without applying HTTP\
                  \ protocol optimization, byte-caching, or web caching. TCP protocol\
                  \ optimization is applied.   \nbest-effort:Assume all HTTP sessions\
                  \ comply with HTTP 0.9, 1.0, or 1.1. If a session uses a different\
                  \ HTTP version, it may not parse correctly and the connection may\
                  \ be lost."
                enum:
                - reject
                - tunnel
                - best-effort
                type: string
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Select all entries in a CLI table.\n Access Group: netgrp"
      tags:
      - web-proxy/explicit
    put:
      parameters:
      - description: Possible parameters to go in the body for the request
        in: body
        name: body
        required: true
        schema:
          properties:
            ftp-incoming-port:
              description: Accept incoming FTP-over-HTTP requests on one or more ports
                (0 - 65535, default = 0; use the same as HTTP).
              type: string
            ftp-over-http:
              description: "Enable to proxy FTP-over-HTTP sessions sent from a web\
                \ browser.   \nenable:Enable FTP-over-HTTP sessions.   \ndisable:Disable\
                \ FTP-over-HTTP sessions."
              enum:
              - enable
              - disable
              type: string
            http-incoming-port:
              description: Accept incoming HTTP requests on one or more ports (0 -
                65535, default = 8080).
              type: string
            https-incoming-port:
              description: Accept incoming HTTPS requests on one or more ports (0
                - 65535, default = 0, use the same as HTTP).
              type: string
            https-replacement-message:
              description: "Enable/disable sending the client a replacement message\
                \ for HTTPS requests.   \nenable:Display a replacement message for\
                \ HTTPS requests.   \ndisable:Do not display a replacement message\
                \ for HTTPS requests."
              enum:
              - enable
              - disable
              type: string
            incoming-ip:
              description: Restrict the explicit HTTP proxy to only accept sessions
                from this IP address. An interface must have this IP address.
              format: ipv4
              type: string
            incoming-ip6:
              description: Restrict the explicit web proxy to only accept sessions
                from this IPv6 address. An interface must have this IPv6 address.
              format: ipv6
              type: string
            ipv6-status:
              description: "Enable/disable allowing an IPv6 web proxy destination\
                \ in policies and all IPv6 related entries in this command.   \nenable:Enable\
                \ allowing an IPv6 web proxy destination.   \ndisable:Disable allowing\
                \ an IPv6 web proxy destination."
              enum:
              - enable
              - disable
              type: string
            message-upon-server-error:
              description: "Enable/disable displaying a replacement message when a\
                \ server error is detected.   \nenable:Display a replacement message\
                \ when a server error is detected.   \ndisable:Do not display a replacement\
                \ message when a server error is detected."
              enum:
              - enable
              - disable
              type: string
            outgoing-ip:
              description: Outgoing HTTP requests will have this IP address as their
                source address. An interface must have this IP address.
              format: ipv4
              type: string
            outgoing-ip6:
              description: Outgoing HTTP requests will leave this IPv6. Multiple interfaces
                can be specified. Interfaces must have these IPv6 addresses.
              format: ipv6
              type: string
            pac-file-data:
              description: PAC file contents enclosed in quotes (maximum of 256K bytes).
              type: string
            pac-file-name:
              description: Pac file name.
              maxLength: 63
              type: string
            pac-file-server-port:
              description: Port number that PAC traffic from client web browsers uses
                to connect to the explicit web proxy (0 - 65535, default = 0; use
                the same as HTTP).
              type: string
            pac-file-server-status:
              description: "Enable/disable Proxy Auto-Configuration (PAC) for users\
                \ of this explicit proxy profile.   \nenable:Enable Proxy Auto-Configuration\
                \ (PAC).   \ndisable:Disable Proxy Auto-Configuration (PAC)."
              enum:
              - enable
              - disable
              type: string
            pac-file-url:
              description: PAC file access URL.
              type: string
            pac-policy:
              description: PAC policies.
              items:
                properties:
                  comments:
                    description: Optional comments.
                    maxLength: 1023
                    type: string
                  dstaddr:
                    description: Destination address objects.
                    items:
                      properties:
                        name:
                          description: Address name.
                          maxLength: 79
                          type: string
                      type: object
                    maxItems: null
                    type: array
                  pac-file-data:
                    description: PAC file contents enclosed in quotes (maximum of
                      256K bytes).
                    type: string
                  pac-file-name:
                    description: Pac file name.
                    maxLength: 63
                    type: string
                  policyid:
                    description: Policy ID.
                    maximum: 100
                    minimum: 1
                    type: integer
                  srcaddr:
                    description: Source address objects.
                    items:
                      properties:
                        name:
                          description: Address name.
                          maxLength: 79
                          type: string
                      type: object
                    maxItems: null
                    type: array
                  srcaddr6:
                    description: Source address6 objects.
                    items:
                      properties:
                        name:
                          description: Address name.
                          maxLength: 79
                          type: string
                      type: object
                    maxItems: null
                    type: array
                  status:
                    description: "Enable/disable policy.   \nenable:Enable policy.\
                      \   \ndisable:Disable policy."
                    enum:
                    - enable
                    - disable
                    type: string
                type: object
              maxItems: null
              type: array
            pref-dns-result:
              description: "Prefer resolving addresses using the configured IPv4 or\
                \ IPv6 DNS server (default = ipv4).   \nipv4:Prefer the IPv4 DNS server.\
                \   \nipv6:Prefer the IPv6 DNS server."
              enum:
              - ipv4
              - ipv6
              type: string
            realm:
              description: Authentication realm used to identify the explicit web
                proxy (maximum of 63 characters).
              maxLength: 63
              type: string
            sec-default-action:
              description: "Accept or deny explicit web proxy sessions when no web\
                \ proxy firewall policy exists.   \naccept:Accept requests. All explicit\
                \ web proxy traffic is accepted whether there is an explicit web proxy\
                \ policy or not.   \ndeny:Deny requests unless there is a matching\
                \ explicit web proxy policy."
              enum:
              - accept
              - deny
              type: string
            socks:
              description: "Enable/disable the SOCKS proxy.   \nenable:Enable the\
                \ SOCKS proxy.   \ndisable:Disable the SOCKS proxy."
              enum:
              - enable
              - disable
              type: string
            socks-incoming-port:
              description: Accept incoming SOCKS proxy requests on one or more ports
                (0 - 65535, default = 0; use the same as HTTP).
              type: string
            ssl-algorithm:
              description: "Relative strength of encryption algorithms accepted in\
                \ HTTPS deep scan: high, medium, or low.   \nhigh:High encrption.\
                \ Allow only AES and ChaCha.   \nmedium:Medium encryption. Allow AES,\
                \ ChaCha, 3DES, and RC4.   \nlow:Low encryption. Allow AES, ChaCha,\
                \ 3DES, RC4, and DES."
              enum:
              - high
              - medium
              - low
              type: string
            status:
              description: "Enable/disable the explicit Web proxy for HTTP and HTTPS\
                \ session.   \nenable:Enable the explicit web proxy.   \ndisable:Disable\
                \ the explicit web proxy."
              enum:
              - enable
              - disable
              type: string
            strict-guest:
              description: "Enable/disable strict guest user checking by the explicit\
                \ web proxy.   \nenable:Enable strict guest user checking.   \ndisable:Disable\
                \ strict guest user checking."
              enum:
              - enable
              - disable
              type: string
            trace-auth-no-rsp:
              description: "Enable/disable logging timed-out authentication requests.\
                \   \nenable:Enable logging timed-out authentication requests.   \n\
                disable:Disable logging timed-out authentication requests."
              enum:
              - enable
              - disable
              type: string
            unknown-http-version:
              description: "How to handle HTTP sessions that do not comply with HTTP\
                \ 0.9, 1.0, or 1.1.   \nreject:Reject or tear down HTTP sessions that\
                \ do not use HTTP 0.9, 1.0, or 1.1.   \ntunnel:Pass HTTP traffic that\
                \ does not use HTTP 0.9, 1.0, or 1.1 without applying HTTP protocol\
                \ optimization, byte-caching, or web caching. TCP protocol optimization\
                \ is applied.   \nbest-effort:Assume all HTTP sessions comply with\
                \ HTTP 0.9, 1.0, or 1.1. If a session uses a different HTTP version,\
                \ it may not parse correctly and the connection may be lost."
              enum:
              - reject
              - tunnel
              - best-effort
              type: string
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      - description: "If supported, an action can be specified.\n_move_: Move this\
          \ specific resource.\nWhen *action=move* is set, one of the extra parameters\
          \ (*before*, *after*) must be provided.\n__*Note:*__ If this parameter is\
          \ provided when not supported, the action will be ignored and an \u201C\
          invalid request\u201D error will be returned.\n"
        enum:
        - move
        in: query
        name: action
        required: false
        type: string
      - description: 'If *action=move*, use *before* to specify the ID of the resource
          that this resource will be moved before.

          For example, to move `object 1` to before `object 2`, use:

          __action=move&before=2__

          __*Note:*__ This parameter can only be used when the *action* parameter
          is set to *move*.

          '
        in: query
        name: before
        required: false
        type: string
      - description: 'If *action=move*, use *after* to specify the ID of the resource
          that this resource will be moved after.

          For example, to move `object 1` to after `object 3`, use:

          __action=move&after=3__

          __*Note:*__ This parameter can only be used when the *action* parameter
          is set to *move*.

          '
        in: query
        name: after
        required: false
        type: string
      responses:
        '200':
          description: 'OK: Request returns successful'
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Update this specific resource.\n Access Group: netgrp"
      tags:
      - web-proxy/explicit
  /web-proxy/forward-server:
    get:
      parameters:
      - description: Enable to include datasource information for each linked object.
        in: query
        name: datasource
        required: false
        type: boolean
      - description: Starting entry index.
        in: query
        name: start
        required: false
        type: integer
      - description: Maximum number of entries to return.
        in: query
        name: count
        required: false
        type: integer
      - description: Enable to include meta information about each object (type id,
          references, etc).
        in: query
        name: with_meta
        required: false
        type: boolean
      - description: Enable to include a checksum of each object's contents.
        in: query
        name: with_contents_hash
        required: false
        type: boolean
      - description: Enable to call CLI skip operator to hide skipped properties.
        in: query
        name: skip
        required: false
        type: boolean
      - collectionFormat: pipes
        description: List of property names to include in results, separated by |
          (i.e. policyid|srcintf).
        in: query
        items:
          type: string
        name: format
        required: false
        type: array
      - collectionFormat: multi
        description: "Filtering multiple key/value pairs\nOperator     |   Description\n\
          ==     |   Case insensitive match with pattern.\n!=     |    Does not match\
          \ with pattern (case insensitive).\n=@     |    Pattern found in object\
          \ value (case insensitive).\n!@     |    \uFEFFPattern not\uFEFF found in\
          \ object value (case insensitive).\n<=     |    Value must be less than\
          \ or equal to \uFEFFpattern\uFEFF.\n<     |    Value must be less than pattern\uFEFF\
          .\n.>=    |    Value must be greater than or equal to \uFEFFpattern\uFEFF\
          .\n.>     |    Value must be greater than \uFEFFpattern\uFEFF.\nLogical\
          \ OR    |    Separate filters using commas ','\nLogical AND    |    Filter\
          \ strings can be combined to create logical AND queries by including multiple\
          \ filters in the request.\nCombining AND and OR    |    You can combine\
          \ AND and OR filters together to create more complex filters.\n"
        in: query
        items:
          type: string
        name: filter
        required: false
        type: array
      - description: If present, objects will be filtered on property with this name.
        in: query
        name: key
        required: false
        type: string
      - description: If present, objects will be filtered on property with this value.
        in: query
        name: pattern
        required: false
        type: string
      - description: Scope [global|vdom|both*]
        in: query
        name: scope
        required: false
        type: string
      - description: Exclude properties/objects with default value
        in: query
        name: exclude-default-values
        required: false
        type: boolean
      - description: 'default: Return the CLI default values for entire CLI tree.

          meta: Return meta data for a specific object, table, or the entire CLI tree.

          schema: Return schema for entire CLI tree.

          '
        enum:
        - default
        - schema
        in: query
        name: action
        required: false
        type: string
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      responses:
        '200':
          description: 'OK: Request returns successful'
          schema:
            properties:
              addr-type:
                description: "Address type of the forwarding proxy server: IP or FQDN.\
                  \   \nip:Use an IP address for the forwarding proxy server.   \n\
                  fqdn:Use the FQDN for the forwarding proxy server."
                enum:
                - ip
                - fqdn
                type: string
              comment:
                description: Comment.
                maxLength: 63
                type: string
              fqdn:
                description: Forward server Fully Qualified Domain Name (FQDN).
                maxLength: 255
                type: string
              healthcheck:
                description: "Enable/disable forward server health checking. Attempts\
                  \ to connect through the remote forwarding server to a destination\
                  \ to verify that the forwarding server is operating normally.  \
                  \ \ndisable:Disable health checking.   \nenable:Enable health checking."
                enum:
                - disable
                - enable
                type: string
              ip:
                description: Forward proxy server IP address.
                format: ipv4
                type: string
              monitor:
                description: URL for forward server health check monitoring (default
                  = http://www.google.com).
                maxLength: 255
                type: string
              name:
                description: Server name.
                maxLength: 63
                type: string
              password:
                description: HTTP authentication password.
                format: password
                type: string
              port:
                description: Port number that the forwarding server expects to receive
                  HTTP sessions on (1 - 65535, default = 3128).
                maximum: 65535
                minimum: 1
                type: integer
              server-down-option:
                description: "Action to take when the forward server is found to be\
                  \ down: block sessions until the server is back up or pass sessions\
                  \ to their destination.   \nblock:Block sessions until the server\
                  \ is back up.   \npass:Pass sessions to their destination bypassing\
                  \ the forward server."
                enum:
                - block
                - pass
                type: string
              username:
                description: HTTP authentication user name.
                maxLength: 64
                type: string
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Select all entries in a CLI table.\n Access Group: netgrp"
      tags:
      - web-proxy/forward-server
    post:
      parameters:
      - description: Possible parameters to go in the body for the request
        in: body
        name: body
        required: true
        schema:
          properties:
            addr-type:
              description: "Address type of the forwarding proxy server: IP or FQDN.\
                \   \nip:Use an IP address for the forwarding proxy server.   \nfqdn:Use\
                \ the FQDN for the forwarding proxy server."
              enum:
              - ip
              - fqdn
              type: string
            comment:
              description: Comment.
              maxLength: 63
              type: string
            fqdn:
              description: Forward server Fully Qualified Domain Name (FQDN).
              maxLength: 255
              type: string
            healthcheck:
              description: "Enable/disable forward server health checking. Attempts\
                \ to connect through the remote forwarding server to a destination\
                \ to verify that the forwarding server is operating normally.   \n\
                disable:Disable health checking.   \nenable:Enable health checking."
              enum:
              - disable
              - enable
              type: string
            ip:
              description: Forward proxy server IP address.
              format: ipv4
              type: string
            monitor:
              description: URL for forward server health check monitoring (default
                = http://www.google.com).
              maxLength: 255
              type: string
            name:
              description: Server name.
              maxLength: 63
              type: string
            password:
              description: HTTP authentication password.
              format: password
              type: string
            port:
              description: Port number that the forwarding server expects to receive
                HTTP sessions on (1 - 65535, default = 3128).
              maximum: 65535
              minimum: 1
              type: integer
            server-down-option:
              description: "Action to take when the forward server is found to be\
                \ down: block sessions until the server is back up or pass sessions\
                \ to their destination.   \nblock:Block sessions until the server\
                \ is back up.   \npass:Pass sessions to their destination bypassing\
                \ the forward server."
              enum:
              - block
              - pass
              type: string
            username:
              description: HTTP authentication user name.
              maxLength: 64
              type: string
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      - description: "If supported, an action can be specified.\n_clone_: Clone this\
          \ specific resource.\nWhen *action=clone* is set, the extra parameters *nkey*\
          \ must be provided.\n__*Note:*__ If this parameter is provided when not\
          \ supported, the action will be ignored and an \u201Cinvalid request\u201D\
          \ error will be returned.\n"
        enum:
        - clone
        in: query
        name: action
        required: false
        type: string
      - description: ' If *action=clone*, use *nkey* to specify the ID for the new
          resource to be created.

          For example, to clone `address1` to `address1_clone`, use:

          __action=clone&nkey=address1_clone__

          __*Note:*__ This parameter can only be used when the *action* parameter
          is set to *clone*.

          '
        in: query
        name: nkey
        required: false
        type: string
      responses:
        '200':
          description: 'OK: Request returns successful'
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Create object(s) in this table.\n Access Group: netgrp"
      tags:
      - web-proxy/forward-server
  /web-proxy/forward-server-group:
    get:
      parameters:
      - description: Enable to include datasource information for each linked object.
        in: query
        name: datasource
        required: false
        type: boolean
      - description: Starting entry index.
        in: query
        name: start
        required: false
        type: integer
      - description: Maximum number of entries to return.
        in: query
        name: count
        required: false
        type: integer
      - description: Enable to include meta information about each object (type id,
          references, etc).
        in: query
        name: with_meta
        required: false
        type: boolean
      - description: Enable to include a checksum of each object's contents.
        in: query
        name: with_contents_hash
        required: false
        type: boolean
      - description: Enable to call CLI skip operator to hide skipped properties.
        in: query
        name: skip
        required: false
        type: boolean
      - collectionFormat: pipes
        description: List of property names to include in results, separated by |
          (i.e. policyid|srcintf).
        in: query
        items:
          type: string
        name: format
        required: false
        type: array
      - collectionFormat: multi
        description: "Filtering multiple key/value pairs\nOperator     |   Description\n\
          ==     |   Case insensitive match with pattern.\n!=     |    Does not match\
          \ with pattern (case insensitive).\n=@     |    Pattern found in object\
          \ value (case insensitive).\n!@     |    \uFEFFPattern not\uFEFF found in\
          \ object value (case insensitive).\n<=     |    Value must be less than\
          \ or equal to \uFEFFpattern\uFEFF.\n<     |    Value must be less than pattern\uFEFF\
          .\n.>=    |    Value must be greater than or equal to \uFEFFpattern\uFEFF\
          .\n.>     |    Value must be greater than \uFEFFpattern\uFEFF.\nLogical\
          \ OR    |    Separate filters using commas ','\nLogical AND    |    Filter\
          \ strings can be combined to create logical AND queries by including multiple\
          \ filters in the request.\nCombining AND and OR    |    You can combine\
          \ AND and OR filters together to create more complex filters.\n"
        in: query
        items:
          type: string
        name: filter
        required: false
        type: array
      - description: If present, objects will be filtered on property with this name.
        in: query
        name: key
        required: false
        type: string
      - description: If present, objects will be filtered on property with this value.
        in: query
        name: pattern
        required: false
        type: string
      - description: Scope [global|vdom|both*]
        in: query
        name: scope
        required: false
        type: string
      - description: Exclude properties/objects with default value
        in: query
        name: exclude-default-values
        required: false
        type: boolean
      - description: 'default: Return the CLI default values for entire CLI tree.

          meta: Return meta data for a specific object, table, or the entire CLI tree.

          schema: Return schema for entire CLI tree.

          '
        enum:
        - default
        - schema
        in: query
        name: action
        required: false
        type: string
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      responses:
        '200':
          description: 'OK: Request returns successful'
          schema:
            properties:
              affinity:
                description: "Enable/disable affinity, attaching a source-ip's traffic\
                  \ to the assigned forwarding server until the forward-server-affinity-timeout\
                  \ is reached (under web-proxy global).   \nenable:Enable affinity.\
                  \   \ndisable:Disable affinity."
                enum:
                - enable
                - disable
                type: string
              group-down-option:
                description: "Action to take when all of the servers in the forward\
                  \ server group are down: block sessions until at least one server\
                  \ is back up or pass sessions to their destination.   \nblock:Block\
                  \ sessions until at least one server in the group is back up.  \
                  \ \npass:Pass sessions to their destination bypassing servers in\
                  \ the forward server group."
                enum:
                - block
                - pass
                type: string
              ldb-method:
                description: "Load balance method: weighted or least-session.   \n\
                  weighted:Load balance traffic to forward servers based on assigned\
                  \ weights. Weights are ratios of total number of sessions.   \n\
                  least-session:Send new sessions to the server with lowest session\
                  \ count.   \nactive-passive:Send new sessions to the next active\
                  \ server in the list. Servers are selected with highest weight first\
                  \ and then in order as they are configured. Traffic switches back\
                  \ to the first server upon failure recovery."
                enum:
                - weighted
                - least-session
                - active-passive
                type: string
              name:
                description: Configure a forward server group consisting one or multiple
                  forward servers. Supports failover and load balancing.
                maxLength: 63
                type: string
              server-list:
                description: Add web forward servers to a list to form a server group.
                  Optionally assign weights to each server.
                items:
                  properties:
                    name:
                      description: Forward server name.
                      maxLength: 63
                      type: string
                    weight:
                      description: Optionally assign a weight of the forwarding server
                        for weighted load balancing (1 - 100, default = 10)
                      maximum: 100
                      minimum: 1
                      type: integer
                  type: object
                maxItems: null
                type: array
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Select all entries in a CLI table.\n Access Group: netgrp"
      tags:
      - web-proxy/forward-server-group
    post:
      parameters:
      - description: Possible parameters to go in the body for the request
        in: body
        name: body
        required: true
        schema:
          properties:
            affinity:
              description: "Enable/disable affinity, attaching a source-ip's traffic\
                \ to the assigned forwarding server until the forward-server-affinity-timeout\
                \ is reached (under web-proxy global).   \nenable:Enable affinity.\
                \   \ndisable:Disable affinity."
              enum:
              - enable
              - disable
              type: string
            group-down-option:
              description: "Action to take when all of the servers in the forward\
                \ server group are down: block sessions until at least one server\
                \ is back up or pass sessions to their destination.   \nblock:Block\
                \ sessions until at least one server in the group is back up.   \n\
                pass:Pass sessions to their destination bypassing servers in the forward\
                \ server group."
              enum:
              - block
              - pass
              type: string
            ldb-method:
              description: "Load balance method: weighted or least-session.   \nweighted:Load\
                \ balance traffic to forward servers based on assigned weights. Weights\
                \ are ratios of total number of sessions.   \nleast-session:Send new\
                \ sessions to the server with lowest session count.   \nactive-passive:Send\
                \ new sessions to the next active server in the list. Servers are\
                \ selected with highest weight first and then in order as they are\
                \ configured. Traffic switches back to the first server upon failure\
                \ recovery."
              enum:
              - weighted
              - least-session
              - active-passive
              type: string
            name:
              description: Configure a forward server group consisting one or multiple
                forward servers. Supports failover and load balancing.
              maxLength: 63
              type: string
            server-list:
              description: Add web forward servers to a list to form a server group.
                Optionally assign weights to each server.
              items:
                properties:
                  name:
                    description: Forward server name.
                    maxLength: 63
                    type: string
                  weight:
                    description: Optionally assign a weight of the forwarding server
                      for weighted load balancing (1 - 100, default = 10)
                    maximum: 100
                    minimum: 1
                    type: integer
                type: object
              maxItems: null
              type: array
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      - description: "If supported, an action can be specified.\n_clone_: Clone this\
          \ specific resource.\nWhen *action=clone* is set, the extra parameters *nkey*\
          \ must be provided.\n__*Note:*__ If this parameter is provided when not\
          \ supported, the action will be ignored and an \u201Cinvalid request\u201D\
          \ error will be returned.\n"
        enum:
        - clone
        in: query
        name: action
        required: false
        type: string
      - description: ' If *action=clone*, use *nkey* to specify the ID for the new
          resource to be created.

          For example, to clone `address1` to `address1_clone`, use:

          __action=clone&nkey=address1_clone__

          __*Note:*__ This parameter can only be used when the *action* parameter
          is set to *clone*.

          '
        in: query
        name: nkey
        required: false
        type: string
      responses:
        '200':
          description: 'OK: Request returns successful'
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Create object(s) in this table.\n Access Group: netgrp"
      tags:
      - web-proxy/forward-server-group
  /web-proxy/forward-server-group/{name}:
    delete:
      parameters:
      - description: mkey
        in: path
        name: name
        required: true
        type: string
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      responses:
        '200':
          description: 'OK: Request returns successful'
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Delete this specific resource.\n Access Group: netgrp"
      tags:
      - web-proxy/forward-server-group
    get:
      parameters:
      - description: Enable to include datasource information for each linked object.
        in: query
        name: datasource
        required: false
        type: boolean
      - description: Enable to include meta information about each object (type id,
          references, etc).
        in: query
        name: with_meta
        required: false
        type: boolean
      - description: Enable to call CLI skip operator to hide skipped properties.
        in: query
        name: skip
        required: false
        type: boolean
      - collectionFormat: pipes
        description: List of property names to include in results, separated by |
          (i.e. policyid|srcintf).
        in: query
        items:
          type: string
        name: format
        required: false
        type: array
      - description: 'default: Return the CLI default values for this object type.

          schema: Return the CLI schema for this object type.

          revision: Return the CMDB revision for this object type.

          transaction-list: List all configuration transaction(s).

          '
        enum:
        - default
        - schema
        - revision
        in: query
        name: action
        required: false
        type: string
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      - description: mkey
        in: path
        name: name
        required: true
        type: string
      responses:
        '200':
          description: 'OK: Request returns successful'
          schema:
            properties:
              affinity:
                description: "Enable/disable affinity, attaching a source-ip's traffic\
                  \ to the assigned forwarding server until the forward-server-affinity-timeout\
                  \ is reached (under web-proxy global).   \nenable:Enable affinity.\
                  \   \ndisable:Disable affinity."
                enum:
                - enable
                - disable
                type: string
              group-down-option:
                description: "Action to take when all of the servers in the forward\
                  \ server group are down: block sessions until at least one server\
                  \ is back up or pass sessions to their destination.   \nblock:Block\
                  \ sessions until at least one server in the group is back up.  \
                  \ \npass:Pass sessions to their destination bypassing servers in\
                  \ the forward server group."
                enum:
                - block
                - pass
                type: string
              ldb-method:
                description: "Load balance method: weighted or least-session.   \n\
                  weighted:Load balance traffic to forward servers based on assigned\
                  \ weights. Weights are ratios of total number of sessions.   \n\
                  least-session:Send new sessions to the server with lowest session\
                  \ count.   \nactive-passive:Send new sessions to the next active\
                  \ server in the list. Servers are selected with highest weight first\
                  \ and then in order as they are configured. Traffic switches back\
                  \ to the first server upon failure recovery."
                enum:
                - weighted
                - least-session
                - active-passive
                type: string
              name:
                description: Configure a forward server group consisting one or multiple
                  forward servers. Supports failover and load balancing.
                maxLength: 63
                type: string
              server-list:
                description: Add web forward servers to a list to form a server group.
                  Optionally assign weights to each server.
                items:
                  properties:
                    name:
                      description: Forward server name.
                      maxLength: 63
                      type: string
                    weight:
                      description: Optionally assign a weight of the forwarding server
                        for weighted load balancing (1 - 100, default = 10)
                      maximum: 100
                      minimum: 1
                      type: integer
                  type: object
                maxItems: null
                type: array
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Select a specific entry from a CLI table.\n Access Group: netgrp"
      tags:
      - web-proxy/forward-server-group
    put:
      parameters:
      - description: Possible parameters to go in the body for the request
        in: body
        name: body
        required: true
        schema:
          properties:
            affinity:
              description: "Enable/disable affinity, attaching a source-ip's traffic\
                \ to the assigned forwarding server until the forward-server-affinity-timeout\
                \ is reached (under web-proxy global).   \nenable:Enable affinity.\
                \   \ndisable:Disable affinity."
              enum:
              - enable
              - disable
              type: string
            group-down-option:
              description: "Action to take when all of the servers in the forward\
                \ server group are down: block sessions until at least one server\
                \ is back up or pass sessions to their destination.   \nblock:Block\
                \ sessions until at least one server in the group is back up.   \n\
                pass:Pass sessions to their destination bypassing servers in the forward\
                \ server group."
              enum:
              - block
              - pass
              type: string
            ldb-method:
              description: "Load balance method: weighted or least-session.   \nweighted:Load\
                \ balance traffic to forward servers based on assigned weights. Weights\
                \ are ratios of total number of sessions.   \nleast-session:Send new\
                \ sessions to the server with lowest session count.   \nactive-passive:Send\
                \ new sessions to the next active server in the list. Servers are\
                \ selected with highest weight first and then in order as they are\
                \ configured. Traffic switches back to the first server upon failure\
                \ recovery."
              enum:
              - weighted
              - least-session
              - active-passive
              type: string
            name:
              description: Configure a forward server group consisting one or multiple
                forward servers. Supports failover and load balancing.
              maxLength: 63
              type: string
            server-list:
              description: Add web forward servers to a list to form a server group.
                Optionally assign weights to each server.
              items:
                properties:
                  name:
                    description: Forward server name.
                    maxLength: 63
                    type: string
                  weight:
                    description: Optionally assign a weight of the forwarding server
                      for weighted load balancing (1 - 100, default = 10)
                    maximum: 100
                    minimum: 1
                    type: integer
                type: object
              maxItems: null
              type: array
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      - description: "If supported, an action can be specified.\n_move_: Move this\
          \ specific resource.\nWhen *action=move* is set, one of the extra parameters\
          \ (*before*, *after*) must be provided.\n__*Note:*__ If this parameter is\
          \ provided when not supported, the action will be ignored and an \u201C\
          invalid request\u201D error will be returned.\n"
        enum:
        - move
        in: query
        name: action
        required: false
        type: string
      - description: 'If *action=move*, use *before* to specify the ID of the resource
          that this resource will be moved before.

          For example, to move `object 1` to before `object 2`, use:

          __action=move&before=2__

          __*Note:*__ This parameter can only be used when the *action* parameter
          is set to *move*.

          '
        in: query
        name: before
        required: false
        type: string
      - description: 'If *action=move*, use *after* to specify the ID of the resource
          that this resource will be moved after.

          For example, to move `object 1` to after `object 3`, use:

          __action=move&after=3__

          __*Note:*__ This parameter can only be used when the *action* parameter
          is set to *move*.

          '
        in: query
        name: after
        required: false
        type: string
      - description: mkey
        in: path
        name: name
        required: true
        type: string
      responses:
        '200':
          description: 'OK: Request returns successful'
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Update this specific resource.\n Access Group: netgrp"
      tags:
      - web-proxy/forward-server-group
  /web-proxy/forward-server/{name}:
    delete:
      parameters:
      - description: mkey
        in: path
        name: name
        required: true
        type: string
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      responses:
        '200':
          description: 'OK: Request returns successful'
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Delete this specific resource.\n Access Group: netgrp"
      tags:
      - web-proxy/forward-server
    get:
      parameters:
      - description: Enable to include datasource information for each linked object.
        in: query
        name: datasource
        required: false
        type: boolean
      - description: Enable to include meta information about each object (type id,
          references, etc).
        in: query
        name: with_meta
        required: false
        type: boolean
      - description: Enable to call CLI skip operator to hide skipped properties.
        in: query
        name: skip
        required: false
        type: boolean
      - collectionFormat: pipes
        description: List of property names to include in results, separated by |
          (i.e. policyid|srcintf).
        in: query
        items:
          type: string
        name: format
        required: false
        type: array
      - description: 'default: Return the CLI default values for this object type.

          schema: Return the CLI schema for this object type.

          revision: Return the CMDB revision for this object type.

          transaction-list: List all configuration transaction(s).

          '
        enum:
        - default
        - schema
        - revision
        in: query
        name: action
        required: false
        type: string
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      - description: mkey
        in: path
        name: name
        required: true
        type: string
      responses:
        '200':
          description: 'OK: Request returns successful'
          schema:
            properties:
              addr-type:
                description: "Address type of the forwarding proxy server: IP or FQDN.\
                  \   \nip:Use an IP address for the forwarding proxy server.   \n\
                  fqdn:Use the FQDN for the forwarding proxy server."
                enum:
                - ip
                - fqdn
                type: string
              comment:
                description: Comment.
                maxLength: 63
                type: string
              fqdn:
                description: Forward server Fully Qualified Domain Name (FQDN).
                maxLength: 255
                type: string
              healthcheck:
                description: "Enable/disable forward server health checking. Attempts\
                  \ to connect through the remote forwarding server to a destination\
                  \ to verify that the forwarding server is operating normally.  \
                  \ \ndisable:Disable health checking.   \nenable:Enable health checking."
                enum:
                - disable
                - enable
                type: string
              ip:
                description: Forward proxy server IP address.
                format: ipv4
                type: string
              monitor:
                description: URL for forward server health check monitoring (default
                  = http://www.google.com).
                maxLength: 255
                type: string
              name:
                description: Server name.
                maxLength: 63
                type: string
              password:
                description: HTTP authentication password.
                format: password
                type: string
              port:
                description: Port number that the forwarding server expects to receive
                  HTTP sessions on (1 - 65535, default = 3128).
                maximum: 65535
                minimum: 1
                type: integer
              server-down-option:
                description: "Action to take when the forward server is found to be\
                  \ down: block sessions until the server is back up or pass sessions\
                  \ to their destination.   \nblock:Block sessions until the server\
                  \ is back up.   \npass:Pass sessions to their destination bypassing\
                  \ the forward server."
                enum:
                - block
                - pass
                type: string
              username:
                description: HTTP authentication user name.
                maxLength: 64
                type: string
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Select a specific entry from a CLI table.\n Access Group: netgrp"
      tags:
      - web-proxy/forward-server
    put:
      parameters:
      - description: Possible parameters to go in the body for the request
        in: body
        name: body
        required: true
        schema:
          properties:
            addr-type:
              description: "Address type of the forwarding proxy server: IP or FQDN.\
                \   \nip:Use an IP address for the forwarding proxy server.   \nfqdn:Use\
                \ the FQDN for the forwarding proxy server."
              enum:
              - ip
              - fqdn
              type: string
            comment:
              description: Comment.
              maxLength: 63
              type: string
            fqdn:
              description: Forward server Fully Qualified Domain Name (FQDN).
              maxLength: 255
              type: string
            healthcheck:
              description: "Enable/disable forward server health checking. Attempts\
                \ to connect through the remote forwarding server to a destination\
                \ to verify that the forwarding server is operating normally.   \n\
                disable:Disable health checking.   \nenable:Enable health checking."
              enum:
              - disable
              - enable
              type: string
            ip:
              description: Forward proxy server IP address.
              format: ipv4
              type: string
            monitor:
              description: URL for forward server health check monitoring (default
                = http://www.google.com).
              maxLength: 255
              type: string
            name:
              description: Server name.
              maxLength: 63
              type: string
            password:
              description: HTTP authentication password.
              format: password
              type: string
            port:
              description: Port number that the forwarding server expects to receive
                HTTP sessions on (1 - 65535, default = 3128).
              maximum: 65535
              minimum: 1
              type: integer
            server-down-option:
              description: "Action to take when the forward server is found to be\
                \ down: block sessions until the server is back up or pass sessions\
                \ to their destination.   \nblock:Block sessions until the server\
                \ is back up.   \npass:Pass sessions to their destination bypassing\
                \ the forward server."
              enum:
              - block
              - pass
              type: string
            username:
              description: HTTP authentication user name.
              maxLength: 64
              type: string
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      - description: "If supported, an action can be specified.\n_move_: Move this\
          \ specific resource.\nWhen *action=move* is set, one of the extra parameters\
          \ (*before*, *after*) must be provided.\n__*Note:*__ If this parameter is\
          \ provided when not supported, the action will be ignored and an \u201C\
          invalid request\u201D error will be returned.\n"
        enum:
        - move
        in: query
        name: action
        required: false
        type: string
      - description: 'If *action=move*, use *before* to specify the ID of the resource
          that this resource will be moved before.

          For example, to move `object 1` to before `object 2`, use:

          __action=move&before=2__

          __*Note:*__ This parameter can only be used when the *action* parameter
          is set to *move*.

          '
        in: query
        name: before
        required: false
        type: string
      - description: 'If *action=move*, use *after* to specify the ID of the resource
          that this resource will be moved after.

          For example, to move `object 1` to after `object 3`, use:

          __action=move&after=3__

          __*Note:*__ This parameter can only be used when the *action* parameter
          is set to *move*.

          '
        in: query
        name: after
        required: false
        type: string
      - description: mkey
        in: path
        name: name
        required: true
        type: string
      responses:
        '200':
          description: 'OK: Request returns successful'
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Update this specific resource.\n Access Group: netgrp"
      tags:
      - web-proxy/forward-server
  /web-proxy/global:
    get:
      parameters:
      - description: Enable to include datasource information for each linked object.
        in: query
        name: datasource
        required: false
        type: boolean
      - description: Starting entry index.
        in: query
        name: start
        required: false
        type: integer
      - description: Maximum number of entries to return.
        in: query
        name: count
        required: false
        type: integer
      - description: Enable to include meta information about each object (type id,
          references, etc).
        in: query
        name: with_meta
        required: false
        type: boolean
      - description: Enable to include a checksum of each object's contents.
        in: query
        name: with_contents_hash
        required: false
        type: boolean
      - description: Enable to call CLI skip operator to hide skipped properties.
        in: query
        name: skip
        required: false
        type: boolean
      - collectionFormat: pipes
        description: List of property names to include in results, separated by |
          (i.e. policyid|srcintf).
        in: query
        items:
          type: string
        name: format
        required: false
        type: array
      - collectionFormat: multi
        description: "Filtering multiple key/value pairs\nOperator     |   Description\n\
          ==     |   Case insensitive match with pattern.\n!=     |    Does not match\
          \ with pattern (case insensitive).\n=@     |    Pattern found in object\
          \ value (case insensitive).\n!@     |    \uFEFFPattern not\uFEFF found in\
          \ object value (case insensitive).\n<=     |    Value must be less than\
          \ or equal to \uFEFFpattern\uFEFF.\n<     |    Value must be less than pattern\uFEFF\
          .\n.>=    |    Value must be greater than or equal to \uFEFFpattern\uFEFF\
          .\n.>     |    Value must be greater than \uFEFFpattern\uFEFF.\nLogical\
          \ OR    |    Separate filters using commas ','\nLogical AND    |    Filter\
          \ strings can be combined to create logical AND queries by including multiple\
          \ filters in the request.\nCombining AND and OR    |    You can combine\
          \ AND and OR filters together to create more complex filters.\n"
        in: query
        items:
          type: string
        name: filter
        required: false
        type: array
      - description: If present, objects will be filtered on property with this name.
        in: query
        name: key
        required: false
        type: string
      - description: If present, objects will be filtered on property with this value.
        in: query
        name: pattern
        required: false
        type: string
      - description: Scope [global|vdom|both*]
        in: query
        name: scope
        required: false
        type: string
      - description: Exclude properties/objects with default value
        in: query
        name: exclude-default-values
        required: false
        type: boolean
      - description: 'default: Return the CLI default values for entire CLI tree.

          meta: Return meta data for a specific object, table, or the entire CLI tree.

          schema: Return schema for entire CLI tree.

          '
        enum:
        - default
        - schema
        in: query
        name: action
        required: false
        type: string
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      responses:
        '200':
          description: 'OK: Request returns successful'
          schema:
            properties:
              fast-policy-match:
                description: "Enable/disable fast matching algorithm for explicit\
                  \ and transparent proxy policy.   \nenable:Enable setting.   \n\
                  disable:Disable setting."
                enum:
                - enable
                - disable
                type: string
              forward-proxy-auth:
                description: "Enable/disable forwarding proxy authentication headers.\
                  \   \nenable:Enable forwarding proxy authentication headers.   \n\
                  disable:Disable forwarding proxy authentication headers."
                enum:
                - enable
                - disable
                type: string
              forward-server-affinity-timeout:
                description: Period of time before the source IP's traffic is no longer
                  assigned to the forwarding server (6 - 60 min, default = 30).
                maximum: 60
                minimum: 6
                type: integer
              learn-client-ip:
                description: "Enable/disable learning the client's IP address from\
                  \ headers.   \nenable:Enable learning the client's IP address from\
                  \ headers.   \ndisable:Disable learning the client's IP address\
                  \ from headers."
                enum:
                - enable
                - disable
                type: string
              learn-client-ip-from-header:
                description: "Learn client IP address from the specified headers.\
                  \   \ntrue-client-ip:Learn the client IP address from the True-Client-IP\
                  \ header.   \nx-real-ip:Learn the client IP address from the X-Real-IP\
                  \ header.   \nx-forwarded-for:Learn the client IP address from the\
                  \ X-Forwarded-For header."
                enum:
                - true-client-ip
                - x-real-ip
                - x-forwarded-for
                type: string
              learn-client-ip-srcaddr:
                description: Source address name (srcaddr or srcaddr6 must be set).
                items:
                  properties:
                    name:
                      description: Address name.
                      maxLength: 79
                      type: string
                  type: object
                maxItems: null
                type: array
              learn-client-ip-srcaddr6:
                description: IPv6 Source address name (srcaddr or srcaddr6 must be
                  set).
                items:
                  properties:
                    name:
                      description: Address name.
                      maxLength: 79
                      type: string
                  type: object
                maxItems: null
                type: array
              max-message-length:
                description: Maximum length of HTTP message, not including body (16
                  - 256 Kbytes, default = 32).
                maximum: 256
                minimum: 16
                type: integer
              max-request-length:
                description: Maximum length of HTTP request line (2 - 64 Kbytes, default
                  = 8).
                maximum: 64
                minimum: 2
                type: integer
              max-waf-body-cache-length:
                description: Maximum length of HTTP messages processed by Web Application
                  Firewall (WAF) (10 - 1024 Kbytes, default = 32).
                maximum: 1024
                minimum: 10
                type: integer
              proxy-fqdn:
                description: Fully Qualified Domain Name (FQDN) that clients connect
                  to (default = default.fqdn) to connect to the explicit web proxy.
                maxLength: 255
                type: string
              ssl-ca-cert:
                description: SSL CA certificate for SSL interception.
                maxLength: 35
                type: string
              ssl-cert:
                description: SSL certificate for SSL interception.
                maxLength: 35
                type: string
              strict-web-check:
                description: "Enable/disable strict web checking to block web sites\
                  \ that send incorrect headers that don't conform to HTTP 1.1.  \
                  \ \nenable:Enable strict web checking.   \ndisable:Disable strict\
                  \ web checking."
                enum:
                - enable
                - disable
                type: string
              webproxy-profile:
                description: Name of the web proxy profile to apply when explicit
                  proxy traffic is allowed by default and traffic is accepted that
                  does not match an explicit proxy policy.
                maxLength: 63
                type: string
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Select all entries in a CLI table.\n Access Group: netgrp"
      tags:
      - web-proxy/global
    put:
      parameters:
      - description: Possible parameters to go in the body for the request
        in: body
        name: body
        required: true
        schema:
          properties:
            fast-policy-match:
              description: "Enable/disable fast matching algorithm for explicit and\
                \ transparent proxy policy.   \nenable:Enable setting.   \ndisable:Disable\
                \ setting."
              enum:
              - enable
              - disable
              type: string
            forward-proxy-auth:
              description: "Enable/disable forwarding proxy authentication headers.\
                \   \nenable:Enable forwarding proxy authentication headers.   \n\
                disable:Disable forwarding proxy authentication headers."
              enum:
              - enable
              - disable
              type: string
            forward-server-affinity-timeout:
              description: Period of time before the source IP's traffic is no longer
                assigned to the forwarding server (6 - 60 min, default = 30).
              maximum: 60
              minimum: 6
              type: integer
            learn-client-ip:
              description: "Enable/disable learning the client's IP address from headers.\
                \   \nenable:Enable learning the client's IP address from headers.\
                \   \ndisable:Disable learning the client's IP address from headers."
              enum:
              - enable
              - disable
              type: string
            learn-client-ip-from-header:
              description: "Learn client IP address from the specified headers.  \
                \ \ntrue-client-ip:Learn the client IP address from the True-Client-IP\
                \ header.   \nx-real-ip:Learn the client IP address from the X-Real-IP\
                \ header.   \nx-forwarded-for:Learn the client IP address from the\
                \ X-Forwarded-For header."
              enum:
              - true-client-ip
              - x-real-ip
              - x-forwarded-for
              type: string
            learn-client-ip-srcaddr:
              description: Source address name (srcaddr or srcaddr6 must be set).
              items:
                properties:
                  name:
                    description: Address name.
                    maxLength: 79
                    type: string
                type: object
              maxItems: null
              type: array
            learn-client-ip-srcaddr6:
              description: IPv6 Source address name (srcaddr or srcaddr6 must be set).
              items:
                properties:
                  name:
                    description: Address name.
                    maxLength: 79
                    type: string
                type: object
              maxItems: null
              type: array
            max-message-length:
              description: Maximum length of HTTP message, not including body (16
                - 256 Kbytes, default = 32).
              maximum: 256
              minimum: 16
              type: integer
            max-request-length:
              description: Maximum length of HTTP request line (2 - 64 Kbytes, default
                = 8).
              maximum: 64
              minimum: 2
              type: integer
            max-waf-body-cache-length:
              description: Maximum length of HTTP messages processed by Web Application
                Firewall (WAF) (10 - 1024 Kbytes, default = 32).
              maximum: 1024
              minimum: 10
              type: integer
            proxy-fqdn:
              description: Fully Qualified Domain Name (FQDN) that clients connect
                to (default = default.fqdn) to connect to the explicit web proxy.
              maxLength: 255
              type: string
            ssl-ca-cert:
              description: SSL CA certificate for SSL interception.
              maxLength: 35
              type: string
            ssl-cert:
              description: SSL certificate for SSL interception.
              maxLength: 35
              type: string
            strict-web-check:
              description: "Enable/disable strict web checking to block web sites\
                \ that send incorrect headers that don't conform to HTTP 1.1.   \n\
                enable:Enable strict web checking.   \ndisable:Disable strict web\
                \ checking."
              enum:
              - enable
              - disable
              type: string
            webproxy-profile:
              description: Name of the web proxy profile to apply when explicit proxy
                traffic is allowed by default and traffic is accepted that does not
                match an explicit proxy policy.
              maxLength: 63
              type: string
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      - description: "If supported, an action can be specified.\n_move_: Move this\
          \ specific resource.\nWhen *action=move* is set, one of the extra parameters\
          \ (*before*, *after*) must be provided.\n__*Note:*__ If this parameter is\
          \ provided when not supported, the action will be ignored and an \u201C\
          invalid request\u201D error will be returned.\n"
        enum:
        - move
        in: query
        name: action
        required: false
        type: string
      - description: 'If *action=move*, use *before* to specify the ID of the resource
          that this resource will be moved before.

          For example, to move `object 1` to before `object 2`, use:

          __action=move&before=2__

          __*Note:*__ This parameter can only be used when the *action* parameter
          is set to *move*.

          '
        in: query
        name: before
        required: false
        type: string
      - description: 'If *action=move*, use *after* to specify the ID of the resource
          that this resource will be moved after.

          For example, to move `object 1` to after `object 3`, use:

          __action=move&after=3__

          __*Note:*__ This parameter can only be used when the *action* parameter
          is set to *move*.

          '
        in: query
        name: after
        required: false
        type: string
      responses:
        '200':
          description: 'OK: Request returns successful'
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Update this specific resource.\n Access Group: netgrp"
      tags:
      - web-proxy/global
  /web-proxy/profile:
    get:
      parameters:
      - description: Enable to include datasource information for each linked object.
        in: query
        name: datasource
        required: false
        type: boolean
      - description: Starting entry index.
        in: query
        name: start
        required: false
        type: integer
      - description: Maximum number of entries to return.
        in: query
        name: count
        required: false
        type: integer
      - description: Enable to include meta information about each object (type id,
          references, etc).
        in: query
        name: with_meta
        required: false
        type: boolean
      - description: Enable to include a checksum of each object's contents.
        in: query
        name: with_contents_hash
        required: false
        type: boolean
      - description: Enable to call CLI skip operator to hide skipped properties.
        in: query
        name: skip
        required: false
        type: boolean
      - collectionFormat: pipes
        description: List of property names to include in results, separated by |
          (i.e. policyid|srcintf).
        in: query
        items:
          type: string
        name: format
        required: false
        type: array
      - collectionFormat: multi
        description: "Filtering multiple key/value pairs\nOperator     |   Description\n\
          ==     |   Case insensitive match with pattern.\n!=     |    Does not match\
          \ with pattern (case insensitive).\n=@     |    Pattern found in object\
          \ value (case insensitive).\n!@     |    \uFEFFPattern not\uFEFF found in\
          \ object value (case insensitive).\n<=     |    Value must be less than\
          \ or equal to \uFEFFpattern\uFEFF.\n<     |    Value must be less than pattern\uFEFF\
          .\n.>=    |    Value must be greater than or equal to \uFEFFpattern\uFEFF\
          .\n.>     |    Value must be greater than \uFEFFpattern\uFEFF.\nLogical\
          \ OR    |    Separate filters using commas ','\nLogical AND    |    Filter\
          \ strings can be combined to create logical AND queries by including multiple\
          \ filters in the request.\nCombining AND and OR    |    You can combine\
          \ AND and OR filters together to create more complex filters.\n"
        in: query
        items:
          type: string
        name: filter
        required: false
        type: array
      - description: If present, objects will be filtered on property with this name.
        in: query
        name: key
        required: false
        type: string
      - description: If present, objects will be filtered on property with this value.
        in: query
        name: pattern
        required: false
        type: string
      - description: Scope [global|vdom|both*]
        in: query
        name: scope
        required: false
        type: string
      - description: Exclude properties/objects with default value
        in: query
        name: exclude-default-values
        required: false
        type: boolean
      - description: 'default: Return the CLI default values for entire CLI tree.

          meta: Return meta data for a specific object, table, or the entire CLI tree.

          schema: Return schema for entire CLI tree.

          '
        enum:
        - default
        - schema
        in: query
        name: action
        required: false
        type: string
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      responses:
        '200':
          description: 'OK: Request returns successful'
          schema:
            properties:
              header-client-ip:
                description: "Action to take on the HTTP client-IP header in forwarded\
                  \ requests: forwards (pass), adds, or removes the HTTP header. \
                  \  \npass:Forward the same HTTP header.   \nadd:Add the HTTP header.\
                  \   \nremove:Remove the HTTP header."
                enum:
                - pass
                - add
                - remove
                type: string
              header-front-end-https:
                description: "Action to take on the HTTP front-end-HTTPS header in\
                  \ forwarded requests: forwards (pass), adds, or removes the HTTP\
                  \ header.   \npass:Forward the same HTTP header.   \nadd:Add the\
                  \ HTTP header.   \nremove:Remove the HTTP header."
                enum:
                - pass
                - add
                - remove
                type: string
              header-via-request:
                description: "Action to take on the HTTP via header in forwarded requests:\
                  \ forwards (pass), adds, or removes the HTTP header.   \npass:Forward\
                  \ the same HTTP header.   \nadd:Add the HTTP header.   \nremove:Remove\
                  \ the HTTP header."
                enum:
                - pass
                - add
                - remove
                type: string
              header-via-response:
                description: "Action to take on the HTTP via header in forwarded responses:\
                  \ forwards (pass), adds, or removes the HTTP header.   \npass:Forward\
                  \ the same HTTP header.   \nadd:Add the HTTP header.   \nremove:Remove\
                  \ the HTTP header."
                enum:
                - pass
                - add
                - remove
                type: string
              header-x-authenticated-groups:
                description: "Action to take on the HTTP x-authenticated-groups header\
                  \ in forwarded requests: forwards (pass), adds, or removes the HTTP\
                  \ header.   \npass:Forward the same HTTP header.   \nadd:Add the\
                  \ HTTP header.   \nremove:Remove the HTTP header."
                enum:
                - pass
                - add
                - remove
                type: string
              header-x-authenticated-user:
                description: "Action to take on the HTTP x-authenticated-user header\
                  \ in forwarded requests: forwards (pass), adds, or removes the HTTP\
                  \ header.   \npass:Forward the same HTTP header.   \nadd:Add the\
                  \ HTTP header.   \nremove:Remove the HTTP header."
                enum:
                - pass
                - add
                - remove
                type: string
              header-x-forwarded-for:
                description: "Action to take on the HTTP x-forwarded-for header in\
                  \ forwarded requests: forwards (pass), adds, or removes the HTTP\
                  \ header.   \npass:Forward the same HTTP header.   \nadd:Add the\
                  \ HTTP header.   \nremove:Remove the HTTP header."
                enum:
                - pass
                - add
                - remove
                type: string
              headers:
                description: Configure HTTP forwarded requests headers.
                items:
                  properties:
                    action:
                      description: "Action when the HTTP header is forwarded.   \n\
                        add-to-request:Add the HTTP header to request.   \nadd-to-response:Add\
                        \ the HTTP header to response.   \nremove-from-request:Remove\
                        \ the HTTP header from request.   \nremove-from-response:Remove\
                        \ the HTTP header from response."
                      enum:
                      - add-to-request
                      - add-to-response
                      - remove-from-request
                      - remove-from-response
                      type: string
                    add-option:
                      description: "Configure options to append content to existing\
                        \ HTTP header or add new HTTP header.   \nappend:Append content\
                        \ to existing HTTP header or create new header if HTTP header\
                        \ is not found.   \nnew-on-not-found:Create new header only\
                        \ if existing HTTP header is not found.   \nnew:Create new\
                        \ header regardless if existing HTTP header is found or not."
                      enum:
                      - append
                      - new-on-not-found
                      - new
                      type: string
                    base64-encoding:
                      description: "Enable/disable use of base64 encoding of HTTP\
                        \ content.   \ndisable:Disable use of base64 encoding of HTTP\
                        \ content.   \nenable:Enable use of base64 encoding of HTTP\
                        \ content."
                      enum:
                      - disable
                      - enable
                      type: string
                    content:
                      description: HTTP header content.
                      maxLength: 255
                      type: string
                    dstaddr:
                      description: Destination address and address group names.
                      items:
                        properties:
                          name:
                            description: Address name.
                            maxLength: 79
                            type: string
                        type: object
                      maxItems: null
                      type: array
                    dstaddr6:
                      description: Destination address and address group names (IPv6).
                      items:
                        properties:
                          name:
                            description: Address name.
                            maxLength: 79
                            type: string
                        type: object
                      maxItems: null
                      type: array
                    id:
                      description: HTTP forwarded header id.
                      maximum: 4294967295
                      minimum: 0
                      type: integer
                    name:
                      description: HTTP forwarded header name.
                      maxLength: 79
                      type: string
                    protocol:
                      description: "Configure protocol(s) to take add-option action\
                        \ on (HTTP, HTTPS, or both).   \nhttps:Perform add-option\
                        \ action on HTTPS.   \nhttp:Perform add-option action on HTTP."
                      enum:
                      - https
                      - http
                      type: string
                  type: object
                maxItems: null
                type: array
              log-header-change:
                description: "Enable/disable logging HTTP header changes.   \nenable:Enable\
                  \ Enable/disable logging HTTP header changes.   \ndisable:Disable\
                  \ Enable/disable logging HTTP header changes."
                enum:
                - enable
                - disable
                type: string
              name:
                description: Profile name.
                maxLength: 63
                type: string
              strip-encoding:
                description: "Enable/disable stripping unsupported encoding from the\
                  \ request header.   \nenable:Enable stripping of unsupported encoding\
                  \ from the request header.   \ndisable:Disable stripping of unsupported\
                  \ encoding from the request header."
                enum:
                - enable
                - disable
                type: string
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Select all entries in a CLI table.\n Access Group: netgrp"
      tags:
      - web-proxy/profile
    post:
      parameters:
      - description: Possible parameters to go in the body for the request
        in: body
        name: body
        required: true
        schema:
          properties:
            header-client-ip:
              description: "Action to take on the HTTP client-IP header in forwarded\
                \ requests: forwards (pass), adds, or removes the HTTP header.   \n\
                pass:Forward the same HTTP header.   \nadd:Add the HTTP header.  \
                \ \nremove:Remove the HTTP header."
              enum:
              - pass
              - add
              - remove
              type: string
            header-front-end-https:
              description: "Action to take on the HTTP front-end-HTTPS header in forwarded\
                \ requests: forwards (pass), adds, or removes the HTTP header.   \n\
                pass:Forward the same HTTP header.   \nadd:Add the HTTP header.  \
                \ \nremove:Remove the HTTP header."
              enum:
              - pass
              - add
              - remove
              type: string
            header-via-request:
              description: "Action to take on the HTTP via header in forwarded requests:\
                \ forwards (pass), adds, or removes the HTTP header.   \npass:Forward\
                \ the same HTTP header.   \nadd:Add the HTTP header.   \nremove:Remove\
                \ the HTTP header."
              enum:
              - pass
              - add
              - remove
              type: string
            header-via-response:
              description: "Action to take on the HTTP via header in forwarded responses:\
                \ forwards (pass), adds, or removes the HTTP header.   \npass:Forward\
                \ the same HTTP header.   \nadd:Add the HTTP header.   \nremove:Remove\
                \ the HTTP header."
              enum:
              - pass
              - add
              - remove
              type: string
            header-x-authenticated-groups:
              description: "Action to take on the HTTP x-authenticated-groups header\
                \ in forwarded requests: forwards (pass), adds, or removes the HTTP\
                \ header.   \npass:Forward the same HTTP header.   \nadd:Add the HTTP\
                \ header.   \nremove:Remove the HTTP header."
              enum:
              - pass
              - add
              - remove
              type: string
            header-x-authenticated-user:
              description: "Action to take on the HTTP x-authenticated-user header\
                \ in forwarded requests: forwards (pass), adds, or removes the HTTP\
                \ header.   \npass:Forward the same HTTP header.   \nadd:Add the HTTP\
                \ header.   \nremove:Remove the HTTP header."
              enum:
              - pass
              - add
              - remove
              type: string
            header-x-forwarded-for:
              description: "Action to take on the HTTP x-forwarded-for header in forwarded\
                \ requests: forwards (pass), adds, or removes the HTTP header.   \n\
                pass:Forward the same HTTP header.   \nadd:Add the HTTP header.  \
                \ \nremove:Remove the HTTP header."
              enum:
              - pass
              - add
              - remove
              type: string
            headers:
              description: Configure HTTP forwarded requests headers.
              items:
                properties:
                  action:
                    description: "Action when the HTTP header is forwarded.   \nadd-to-request:Add\
                      \ the HTTP header to request.   \nadd-to-response:Add the HTTP\
                      \ header to response.   \nremove-from-request:Remove the HTTP\
                      \ header from request.   \nremove-from-response:Remove the HTTP\
                      \ header from response."
                    enum:
                    - add-to-request
                    - add-to-response
                    - remove-from-request
                    - remove-from-response
                    type: string
                  add-option:
                    description: "Configure options to append content to existing\
                      \ HTTP header or add new HTTP header.   \nappend:Append content\
                      \ to existing HTTP header or create new header if HTTP header\
                      \ is not found.   \nnew-on-not-found:Create new header only\
                      \ if existing HTTP header is not found.   \nnew:Create new header\
                      \ regardless if existing HTTP header is found or not."
                    enum:
                    - append
                    - new-on-not-found
                    - new
                    type: string
                  base64-encoding:
                    description: "Enable/disable use of base64 encoding of HTTP content.\
                      \   \ndisable:Disable use of base64 encoding of HTTP content.\
                      \   \nenable:Enable use of base64 encoding of HTTP content."
                    enum:
                    - disable
                    - enable
                    type: string
                  content:
                    description: HTTP header content.
                    maxLength: 255
                    type: string
                  dstaddr:
                    description: Destination address and address group names.
                    items:
                      properties:
                        name:
                          description: Address name.
                          maxLength: 79
                          type: string
                      type: object
                    maxItems: null
                    type: array
                  dstaddr6:
                    description: Destination address and address group names (IPv6).
                    items:
                      properties:
                        name:
                          description: Address name.
                          maxLength: 79
                          type: string
                      type: object
                    maxItems: null
                    type: array
                  id:
                    description: HTTP forwarded header id.
                    maximum: 4294967295
                    minimum: 0
                    type: integer
                  name:
                    description: HTTP forwarded header name.
                    maxLength: 79
                    type: string
                  protocol:
                    description: "Configure protocol(s) to take add-option action\
                      \ on (HTTP, HTTPS, or both).   \nhttps:Perform add-option action\
                      \ on HTTPS.   \nhttp:Perform add-option action on HTTP."
                    enum:
                    - https
                    - http
                    type: string
                type: object
              maxItems: null
              type: array
            log-header-change:
              description: "Enable/disable logging HTTP header changes.   \nenable:Enable\
                \ Enable/disable logging HTTP header changes.   \ndisable:Disable\
                \ Enable/disable logging HTTP header changes."
              enum:
              - enable
              - disable
              type: string
            name:
              description: Profile name.
              maxLength: 63
              type: string
            strip-encoding:
              description: "Enable/disable stripping unsupported encoding from the\
                \ request header.   \nenable:Enable stripping of unsupported encoding\
                \ from the request header.   \ndisable:Disable stripping of unsupported\
                \ encoding from the request header."
              enum:
              - enable
              - disable
              type: string
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      - description: "If supported, an action can be specified.\n_clone_: Clone this\
          \ specific resource.\nWhen *action=clone* is set, the extra parameters *nkey*\
          \ must be provided.\n__*Note:*__ If this parameter is provided when not\
          \ supported, the action will be ignored and an \u201Cinvalid request\u201D\
          \ error will be returned.\n"
        enum:
        - clone
        in: query
        name: action
        required: false
        type: string
      - description: ' If *action=clone*, use *nkey* to specify the ID for the new
          resource to be created.

          For example, to clone `address1` to `address1_clone`, use:

          __action=clone&nkey=address1_clone__

          __*Note:*__ This parameter can only be used when the *action* parameter
          is set to *clone*.

          '
        in: query
        name: nkey
        required: false
        type: string
      responses:
        '200':
          description: 'OK: Request returns successful'
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Create object(s) in this table.\n Access Group: netgrp"
      tags:
      - web-proxy/profile
  /web-proxy/profile/{name}:
    delete:
      parameters:
      - description: mkey
        in: path
        name: name
        required: true
        type: string
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      responses:
        '200':
          description: 'OK: Request returns successful'
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Delete this specific resource.\n Access Group: netgrp"
      tags:
      - web-proxy/profile
    get:
      parameters:
      - description: Enable to include datasource information for each linked object.
        in: query
        name: datasource
        required: false
        type: boolean
      - description: Enable to include meta information about each object (type id,
          references, etc).
        in: query
        name: with_meta
        required: false
        type: boolean
      - description: Enable to call CLI skip operator to hide skipped properties.
        in: query
        name: skip
        required: false
        type: boolean
      - collectionFormat: pipes
        description: List of property names to include in results, separated by |
          (i.e. policyid|srcintf).
        in: query
        items:
          type: string
        name: format
        required: false
        type: array
      - description: 'default: Return the CLI default values for this object type.

          schema: Return the CLI schema for this object type.

          revision: Return the CMDB revision for this object type.

          transaction-list: List all configuration transaction(s).

          '
        enum:
        - default
        - schema
        - revision
        in: query
        name: action
        required: false
        type: string
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      - description: mkey
        in: path
        name: name
        required: true
        type: string
      responses:
        '200':
          description: 'OK: Request returns successful'
          schema:
            properties:
              header-client-ip:
                description: "Action to take on the HTTP client-IP header in forwarded\
                  \ requests: forwards (pass), adds, or removes the HTTP header. \
                  \  \npass:Forward the same HTTP header.   \nadd:Add the HTTP header.\
                  \   \nremove:Remove the HTTP header."
                enum:
                - pass
                - add
                - remove
                type: string
              header-front-end-https:
                description: "Action to take on the HTTP front-end-HTTPS header in\
                  \ forwarded requests: forwards (pass), adds, or removes the HTTP\
                  \ header.   \npass:Forward the same HTTP header.   \nadd:Add the\
                  \ HTTP header.   \nremove:Remove the HTTP header."
                enum:
                - pass
                - add
                - remove
                type: string
              header-via-request:
                description: "Action to take on the HTTP via header in forwarded requests:\
                  \ forwards (pass), adds, or removes the HTTP header.   \npass:Forward\
                  \ the same HTTP header.   \nadd:Add the HTTP header.   \nremove:Remove\
                  \ the HTTP header."
                enum:
                - pass
                - add
                - remove
                type: string
              header-via-response:
                description: "Action to take on the HTTP via header in forwarded responses:\
                  \ forwards (pass), adds, or removes the HTTP header.   \npass:Forward\
                  \ the same HTTP header.   \nadd:Add the HTTP header.   \nremove:Remove\
                  \ the HTTP header."
                enum:
                - pass
                - add
                - remove
                type: string
              header-x-authenticated-groups:
                description: "Action to take on the HTTP x-authenticated-groups header\
                  \ in forwarded requests: forwards (pass), adds, or removes the HTTP\
                  \ header.   \npass:Forward the same HTTP header.   \nadd:Add the\
                  \ HTTP header.   \nremove:Remove the HTTP header."
                enum:
                - pass
                - add
                - remove
                type: string
              header-x-authenticated-user:
                description: "Action to take on the HTTP x-authenticated-user header\
                  \ in forwarded requests: forwards (pass), adds, or removes the HTTP\
                  \ header.   \npass:Forward the same HTTP header.   \nadd:Add the\
                  \ HTTP header.   \nremove:Remove the HTTP header."
                enum:
                - pass
                - add
                - remove
                type: string
              header-x-forwarded-for:
                description: "Action to take on the HTTP x-forwarded-for header in\
                  \ forwarded requests: forwards (pass), adds, or removes the HTTP\
                  \ header.   \npass:Forward the same HTTP header.   \nadd:Add the\
                  \ HTTP header.   \nremove:Remove the HTTP header."
                enum:
                - pass
                - add
                - remove
                type: string
              headers:
                description: Configure HTTP forwarded requests headers.
                items:
                  properties:
                    action:
                      description: "Action when the HTTP header is forwarded.   \n\
                        add-to-request:Add the HTTP header to request.   \nadd-to-response:Add\
                        \ the HTTP header to response.   \nremove-from-request:Remove\
                        \ the HTTP header from request.   \nremove-from-response:Remove\
                        \ the HTTP header from response."
                      enum:
                      - add-to-request
                      - add-to-response
                      - remove-from-request
                      - remove-from-response
                      type: string
                    add-option:
                      description: "Configure options to append content to existing\
                        \ HTTP header or add new HTTP header.   \nappend:Append content\
                        \ to existing HTTP header or create new header if HTTP header\
                        \ is not found.   \nnew-on-not-found:Create new header only\
                        \ if existing HTTP header is not found.   \nnew:Create new\
                        \ header regardless if existing HTTP header is found or not."
                      enum:
                      - append
                      - new-on-not-found
                      - new
                      type: string
                    base64-encoding:
                      description: "Enable/disable use of base64 encoding of HTTP\
                        \ content.   \ndisable:Disable use of base64 encoding of HTTP\
                        \ content.   \nenable:Enable use of base64 encoding of HTTP\
                        \ content."
                      enum:
                      - disable
                      - enable
                      type: string
                    content:
                      description: HTTP header content.
                      maxLength: 255
                      type: string
                    dstaddr:
                      description: Destination address and address group names.
                      items:
                        properties:
                          name:
                            description: Address name.
                            maxLength: 79
                            type: string
                        type: object
                      maxItems: null
                      type: array
                    dstaddr6:
                      description: Destination address and address group names (IPv6).
                      items:
                        properties:
                          name:
                            description: Address name.
                            maxLength: 79
                            type: string
                        type: object
                      maxItems: null
                      type: array
                    id:
                      description: HTTP forwarded header id.
                      maximum: 4294967295
                      minimum: 0
                      type: integer
                    name:
                      description: HTTP forwarded header name.
                      maxLength: 79
                      type: string
                    protocol:
                      description: "Configure protocol(s) to take add-option action\
                        \ on (HTTP, HTTPS, or both).   \nhttps:Perform add-option\
                        \ action on HTTPS.   \nhttp:Perform add-option action on HTTP."
                      enum:
                      - https
                      - http
                      type: string
                  type: object
                maxItems: null
                type: array
              log-header-change:
                description: "Enable/disable logging HTTP header changes.   \nenable:Enable\
                  \ Enable/disable logging HTTP header changes.   \ndisable:Disable\
                  \ Enable/disable logging HTTP header changes."
                enum:
                - enable
                - disable
                type: string
              name:
                description: Profile name.
                maxLength: 63
                type: string
              strip-encoding:
                description: "Enable/disable stripping unsupported encoding from the\
                  \ request header.   \nenable:Enable stripping of unsupported encoding\
                  \ from the request header.   \ndisable:Disable stripping of unsupported\
                  \ encoding from the request header."
                enum:
                - enable
                - disable
                type: string
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Select a specific entry from a CLI table.\n Access Group: netgrp"
      tags:
      - web-proxy/profile
    put:
      parameters:
      - description: Possible parameters to go in the body for the request
        in: body
        name: body
        required: true
        schema:
          properties:
            header-client-ip:
              description: "Action to take on the HTTP client-IP header in forwarded\
                \ requests: forwards (pass), adds, or removes the HTTP header.   \n\
                pass:Forward the same HTTP header.   \nadd:Add the HTTP header.  \
                \ \nremove:Remove the HTTP header."
              enum:
              - pass
              - add
              - remove
              type: string
            header-front-end-https:
              description: "Action to take on the HTTP front-end-HTTPS header in forwarded\
                \ requests: forwards (pass), adds, or removes the HTTP header.   \n\
                pass:Forward the same HTTP header.   \nadd:Add the HTTP header.  \
                \ \nremove:Remove the HTTP header."
              enum:
              - pass
              - add
              - remove
              type: string
            header-via-request:
              description: "Action to take on the HTTP via header in forwarded requests:\
                \ forwards (pass), adds, or removes the HTTP header.   \npass:Forward\
                \ the same HTTP header.   \nadd:Add the HTTP header.   \nremove:Remove\
                \ the HTTP header."
              enum:
              - pass
              - add
              - remove
              type: string
            header-via-response:
              description: "Action to take on the HTTP via header in forwarded responses:\
                \ forwards (pass), adds, or removes the HTTP header.   \npass:Forward\
                \ the same HTTP header.   \nadd:Add the HTTP header.   \nremove:Remove\
                \ the HTTP header."
              enum:
              - pass
              - add
              - remove
              type: string
            header-x-authenticated-groups:
              description: "Action to take on the HTTP x-authenticated-groups header\
                \ in forwarded requests: forwards (pass), adds, or removes the HTTP\
                \ header.   \npass:Forward the same HTTP header.   \nadd:Add the HTTP\
                \ header.   \nremove:Remove the HTTP header."
              enum:
              - pass
              - add
              - remove
              type: string
            header-x-authenticated-user:
              description: "Action to take on the HTTP x-authenticated-user header\
                \ in forwarded requests: forwards (pass), adds, or removes the HTTP\
                \ header.   \npass:Forward the same HTTP header.   \nadd:Add the HTTP\
                \ header.   \nremove:Remove the HTTP header."
              enum:
              - pass
              - add
              - remove
              type: string
            header-x-forwarded-for:
              description: "Action to take on the HTTP x-forwarded-for header in forwarded\
                \ requests: forwards (pass), adds, or removes the HTTP header.   \n\
                pass:Forward the same HTTP header.   \nadd:Add the HTTP header.  \
                \ \nremove:Remove the HTTP header."
              enum:
              - pass
              - add
              - remove
              type: string
            headers:
              description: Configure HTTP forwarded requests headers.
              items:
                properties:
                  action:
                    description: "Action when the HTTP header is forwarded.   \nadd-to-request:Add\
                      \ the HTTP header to request.   \nadd-to-response:Add the HTTP\
                      \ header to response.   \nremove-from-request:Remove the HTTP\
                      \ header from request.   \nremove-from-response:Remove the HTTP\
                      \ header from response."
                    enum:
                    - add-to-request
                    - add-to-response
                    - remove-from-request
                    - remove-from-response
                    type: string
                  add-option:
                    description: "Configure options to append content to existing\
                      \ HTTP header or add new HTTP header.   \nappend:Append content\
                      \ to existing HTTP header or create new header if HTTP header\
                      \ is not found.   \nnew-on-not-found:Create new header only\
                      \ if existing HTTP header is not found.   \nnew:Create new header\
                      \ regardless if existing HTTP header is found or not."
                    enum:
                    - append
                    - new-on-not-found
                    - new
                    type: string
                  base64-encoding:
                    description: "Enable/disable use of base64 encoding of HTTP content.\
                      \   \ndisable:Disable use of base64 encoding of HTTP content.\
                      \   \nenable:Enable use of base64 encoding of HTTP content."
                    enum:
                    - disable
                    - enable
                    type: string
                  content:
                    description: HTTP header content.
                    maxLength: 255
                    type: string
                  dstaddr:
                    description: Destination address and address group names.
                    items:
                      properties:
                        name:
                          description: Address name.
                          maxLength: 79
                          type: string
                      type: object
                    maxItems: null
                    type: array
                  dstaddr6:
                    description: Destination address and address group names (IPv6).
                    items:
                      properties:
                        name:
                          description: Address name.
                          maxLength: 79
                          type: string
                      type: object
                    maxItems: null
                    type: array
                  id:
                    description: HTTP forwarded header id.
                    maximum: 4294967295
                    minimum: 0
                    type: integer
                  name:
                    description: HTTP forwarded header name.
                    maxLength: 79
                    type: string
                  protocol:
                    description: "Configure protocol(s) to take add-option action\
                      \ on (HTTP, HTTPS, or both).   \nhttps:Perform add-option action\
                      \ on HTTPS.   \nhttp:Perform add-option action on HTTP."
                    enum:
                    - https
                    - http
                    type: string
                type: object
              maxItems: null
              type: array
            log-header-change:
              description: "Enable/disable logging HTTP header changes.   \nenable:Enable\
                \ Enable/disable logging HTTP header changes.   \ndisable:Disable\
                \ Enable/disable logging HTTP header changes."
              enum:
              - enable
              - disable
              type: string
            name:
              description: Profile name.
              maxLength: 63
              type: string
            strip-encoding:
              description: "Enable/disable stripping unsupported encoding from the\
                \ request header.   \nenable:Enable stripping of unsupported encoding\
                \ from the request header.   \ndisable:Disable stripping of unsupported\
                \ encoding from the request header."
              enum:
              - enable
              - disable
              type: string
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      - description: "If supported, an action can be specified.\n_move_: Move this\
          \ specific resource.\nWhen *action=move* is set, one of the extra parameters\
          \ (*before*, *after*) must be provided.\n__*Note:*__ If this parameter is\
          \ provided when not supported, the action will be ignored and an \u201C\
          invalid request\u201D error will be returned.\n"
        enum:
        - move
        in: query
        name: action
        required: false
        type: string
      - description: 'If *action=move*, use *before* to specify the ID of the resource
          that this resource will be moved before.

          For example, to move `object 1` to before `object 2`, use:

          __action=move&before=2__

          __*Note:*__ This parameter can only be used when the *action* parameter
          is set to *move*.

          '
        in: query
        name: before
        required: false
        type: string
      - description: 'If *action=move*, use *after* to specify the ID of the resource
          that this resource will be moved after.

          For example, to move `object 1` to after `object 3`, use:

          __action=move&after=3__

          __*Note:*__ This parameter can only be used when the *action* parameter
          is set to *move*.

          '
        in: query
        name: after
        required: false
        type: string
      - description: mkey
        in: path
        name: name
        required: true
        type: string
      responses:
        '200':
          description: 'OK: Request returns successful'
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Update this specific resource.\n Access Group: netgrp"
      tags:
      - web-proxy/profile
  /web-proxy/url-match:
    get:
      parameters:
      - description: Enable to include datasource information for each linked object.
        in: query
        name: datasource
        required: false
        type: boolean
      - description: Starting entry index.
        in: query
        name: start
        required: false
        type: integer
      - description: Maximum number of entries to return.
        in: query
        name: count
        required: false
        type: integer
      - description: Enable to include meta information about each object (type id,
          references, etc).
        in: query
        name: with_meta
        required: false
        type: boolean
      - description: Enable to include a checksum of each object's contents.
        in: query
        name: with_contents_hash
        required: false
        type: boolean
      - description: Enable to call CLI skip operator to hide skipped properties.
        in: query
        name: skip
        required: false
        type: boolean
      - collectionFormat: pipes
        description: List of property names to include in results, separated by |
          (i.e. policyid|srcintf).
        in: query
        items:
          type: string
        name: format
        required: false
        type: array
      - collectionFormat: multi
        description: "Filtering multiple key/value pairs\nOperator     |   Description\n\
          ==     |   Case insensitive match with pattern.\n!=     |    Does not match\
          \ with pattern (case insensitive).\n=@     |    Pattern found in object\
          \ value (case insensitive).\n!@     |    \uFEFFPattern not\uFEFF found in\
          \ object value (case insensitive).\n<=     |    Value must be less than\
          \ or equal to \uFEFFpattern\uFEFF.\n<     |    Value must be less than pattern\uFEFF\
          .\n.>=    |    Value must be greater than or equal to \uFEFFpattern\uFEFF\
          .\n.>     |    Value must be greater than \uFEFFpattern\uFEFF.\nLogical\
          \ OR    |    Separate filters using commas ','\nLogical AND    |    Filter\
          \ strings can be combined to create logical AND queries by including multiple\
          \ filters in the request.\nCombining AND and OR    |    You can combine\
          \ AND and OR filters together to create more complex filters.\n"
        in: query
        items:
          type: string
        name: filter
        required: false
        type: array
      - description: If present, objects will be filtered on property with this name.
        in: query
        name: key
        required: false
        type: string
      - description: If present, objects will be filtered on property with this value.
        in: query
        name: pattern
        required: false
        type: string
      - description: Scope [global|vdom|both*]
        in: query
        name: scope
        required: false
        type: string
      - description: Exclude properties/objects with default value
        in: query
        name: exclude-default-values
        required: false
        type: boolean
      - description: 'default: Return the CLI default values for entire CLI tree.

          meta: Return meta data for a specific object, table, or the entire CLI tree.

          schema: Return schema for entire CLI tree.

          '
        enum:
        - default
        - schema
        in: query
        name: action
        required: false
        type: string
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      responses:
        '200':
          description: 'OK: Request returns successful'
          schema:
            properties:
              cache-exemption:
                description: "Enable/disable exempting this URL pattern from caching.\
                  \   \nenable:Enable exempting this URL pattern from caching.   \n\
                  disable:Disable exempting this URL pattern from caching."
                enum:
                - enable
                - disable
                type: string
              comment:
                description: Comment.
                maxLength: 255
                type: string
              forward-server:
                description: Forward server name.
                maxLength: 63
                type: string
              name:
                description: Configure a name for the URL to be exempted.
                maxLength: 63
                type: string
              status:
                description: "Enable/disable exempting the URLs matching the URL pattern\
                  \ from web proxy forwarding and caching.   \nenable:Enable exempting\
                  \ the matching URLs.   \ndisable:Disable exempting the matching\
                  \ URLs."
                enum:
                - enable
                - disable
                type: string
              url-pattern:
                description: URL pattern to be exempted from web proxy forwarding
                  and caching.
                maxLength: 511
                type: string
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Select all entries in a CLI table.\n Access Group: netgrp"
      tags:
      - web-proxy/url-match
    post:
      parameters:
      - description: Possible parameters to go in the body for the request
        in: body
        name: body
        required: true
        schema:
          properties:
            cache-exemption:
              description: "Enable/disable exempting this URL pattern from caching.\
                \   \nenable:Enable exempting this URL pattern from caching.   \n\
                disable:Disable exempting this URL pattern from caching."
              enum:
              - enable
              - disable
              type: string
            comment:
              description: Comment.
              maxLength: 255
              type: string
            forward-server:
              description: Forward server name.
              maxLength: 63
              type: string
            name:
              description: Configure a name for the URL to be exempted.
              maxLength: 63
              type: string
            status:
              description: "Enable/disable exempting the URLs matching the URL pattern\
                \ from web proxy forwarding and caching.   \nenable:Enable exempting\
                \ the matching URLs.   \ndisable:Disable exempting the matching URLs."
              enum:
              - enable
              - disable
              type: string
            url-pattern:
              description: URL pattern to be exempted from web proxy forwarding and
                caching.
              maxLength: 511
              type: string
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      - description: "If supported, an action can be specified.\n_clone_: Clone this\
          \ specific resource.\nWhen *action=clone* is set, the extra parameters *nkey*\
          \ must be provided.\n__*Note:*__ If this parameter is provided when not\
          \ supported, the action will be ignored and an \u201Cinvalid request\u201D\
          \ error will be returned.\n"
        enum:
        - clone
        in: query
        name: action
        required: false
        type: string
      - description: ' If *action=clone*, use *nkey* to specify the ID for the new
          resource to be created.

          For example, to clone `address1` to `address1_clone`, use:

          __action=clone&nkey=address1_clone__

          __*Note:*__ This parameter can only be used when the *action* parameter
          is set to *clone*.

          '
        in: query
        name: nkey
        required: false
        type: string
      responses:
        '200':
          description: 'OK: Request returns successful'
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Create object(s) in this table.\n Access Group: netgrp"
      tags:
      - web-proxy/url-match
  /web-proxy/url-match/{name}:
    delete:
      parameters:
      - description: mkey
        in: path
        name: name
        required: true
        type: string
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      responses:
        '200':
          description: 'OK: Request returns successful'
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Delete this specific resource.\n Access Group: netgrp"
      tags:
      - web-proxy/url-match
    get:
      parameters:
      - description: Enable to include datasource information for each linked object.
        in: query
        name: datasource
        required: false
        type: boolean
      - description: Enable to include meta information about each object (type id,
          references, etc).
        in: query
        name: with_meta
        required: false
        type: boolean
      - description: Enable to call CLI skip operator to hide skipped properties.
        in: query
        name: skip
        required: false
        type: boolean
      - collectionFormat: pipes
        description: List of property names to include in results, separated by |
          (i.e. policyid|srcintf).
        in: query
        items:
          type: string
        name: format
        required: false
        type: array
      - description: 'default: Return the CLI default values for this object type.

          schema: Return the CLI schema for this object type.

          revision: Return the CMDB revision for this object type.

          transaction-list: List all configuration transaction(s).

          '
        enum:
        - default
        - schema
        - revision
        in: query
        name: action
        required: false
        type: string
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      - description: mkey
        in: path
        name: name
        required: true
        type: string
      responses:
        '200':
          description: 'OK: Request returns successful'
          schema:
            properties:
              cache-exemption:
                description: "Enable/disable exempting this URL pattern from caching.\
                  \   \nenable:Enable exempting this URL pattern from caching.   \n\
                  disable:Disable exempting this URL pattern from caching."
                enum:
                - enable
                - disable
                type: string
              comment:
                description: Comment.
                maxLength: 255
                type: string
              forward-server:
                description: Forward server name.
                maxLength: 63
                type: string
              name:
                description: Configure a name for the URL to be exempted.
                maxLength: 63
                type: string
              status:
                description: "Enable/disable exempting the URLs matching the URL pattern\
                  \ from web proxy forwarding and caching.   \nenable:Enable exempting\
                  \ the matching URLs.   \ndisable:Disable exempting the matching\
                  \ URLs."
                enum:
                - enable
                - disable
                type: string
              url-pattern:
                description: URL pattern to be exempted from web proxy forwarding
                  and caching.
                maxLength: 511
                type: string
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Select a specific entry from a CLI table.\n Access Group: netgrp"
      tags:
      - web-proxy/url-match
    put:
      parameters:
      - description: Possible parameters to go in the body for the request
        in: body
        name: body
        required: true
        schema:
          properties:
            cache-exemption:
              description: "Enable/disable exempting this URL pattern from caching.\
                \   \nenable:Enable exempting this URL pattern from caching.   \n\
                disable:Disable exempting this URL pattern from caching."
              enum:
              - enable
              - disable
              type: string
            comment:
              description: Comment.
              maxLength: 255
              type: string
            forward-server:
              description: Forward server name.
              maxLength: 63
              type: string
            name:
              description: Configure a name for the URL to be exempted.
              maxLength: 63
              type: string
            status:
              description: "Enable/disable exempting the URLs matching the URL pattern\
                \ from web proxy forwarding and caching.   \nenable:Enable exempting\
                \ the matching URLs.   \ndisable:Disable exempting the matching URLs."
              enum:
              - enable
              - disable
              type: string
            url-pattern:
              description: URL pattern to be exempted from web proxy forwarding and
                caching.
              maxLength: 511
              type: string
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      - description: "If supported, an action can be specified.\n_move_: Move this\
          \ specific resource.\nWhen *action=move* is set, one of the extra parameters\
          \ (*before*, *after*) must be provided.\n__*Note:*__ If this parameter is\
          \ provided when not supported, the action will be ignored and an \u201C\
          invalid request\u201D error will be returned.\n"
        enum:
        - move
        in: query
        name: action
        required: false
        type: string
      - description: 'If *action=move*, use *before* to specify the ID of the resource
          that this resource will be moved before.

          For example, to move `object 1` to before `object 2`, use:

          __action=move&before=2__

          __*Note:*__ This parameter can only be used when the *action* parameter
          is set to *move*.

          '
        in: query
        name: before
        required: false
        type: string
      - description: 'If *action=move*, use *after* to specify the ID of the resource
          that this resource will be moved after.

          For example, to move `object 1` to after `object 3`, use:

          __action=move&after=3__

          __*Note:*__ This parameter can only be used when the *action* parameter
          is set to *move*.

          '
        in: query
        name: after
        required: false
        type: string
      - description: mkey
        in: path
        name: name
        required: true
        type: string
      responses:
        '200':
          description: 'OK: Request returns successful'
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Update this specific resource.\n Access Group: netgrp"
      tags:
      - web-proxy/url-match
  /web-proxy/wisp:
    get:
      parameters:
      - description: Enable to include datasource information for each linked object.
        in: query
        name: datasource
        required: false
        type: boolean
      - description: Starting entry index.
        in: query
        name: start
        required: false
        type: integer
      - description: Maximum number of entries to return.
        in: query
        name: count
        required: false
        type: integer
      - description: Enable to include meta information about each object (type id,
          references, etc).
        in: query
        name: with_meta
        required: false
        type: boolean
      - description: Enable to include a checksum of each object's contents.
        in: query
        name: with_contents_hash
        required: false
        type: boolean
      - description: Enable to call CLI skip operator to hide skipped properties.
        in: query
        name: skip
        required: false
        type: boolean
      - collectionFormat: pipes
        description: List of property names to include in results, separated by |
          (i.e. policyid|srcintf).
        in: query
        items:
          type: string
        name: format
        required: false
        type: array
      - collectionFormat: multi
        description: "Filtering multiple key/value pairs\nOperator     |   Description\n\
          ==     |   Case insensitive match with pattern.\n!=     |    Does not match\
          \ with pattern (case insensitive).\n=@     |    Pattern found in object\
          \ value (case insensitive).\n!@     |    \uFEFFPattern not\uFEFF found in\
          \ object value (case insensitive).\n<=     |    Value must be less than\
          \ or equal to \uFEFFpattern\uFEFF.\n<     |    Value must be less than pattern\uFEFF\
          .\n.>=    |    Value must be greater than or equal to \uFEFFpattern\uFEFF\
          .\n.>     |    Value must be greater than \uFEFFpattern\uFEFF.\nLogical\
          \ OR    |    Separate filters using commas ','\nLogical AND    |    Filter\
          \ strings can be combined to create logical AND queries by including multiple\
          \ filters in the request.\nCombining AND and OR    |    You can combine\
          \ AND and OR filters together to create more complex filters.\n"
        in: query
        items:
          type: string
        name: filter
        required: false
        type: array
      - description: If present, objects will be filtered on property with this name.
        in: query
        name: key
        required: false
        type: string
      - description: If present, objects will be filtered on property with this value.
        in: query
        name: pattern
        required: false
        type: string
      - description: Scope [global|vdom|both*]
        in: query
        name: scope
        required: false
        type: string
      - description: Exclude properties/objects with default value
        in: query
        name: exclude-default-values
        required: false
        type: boolean
      - description: 'default: Return the CLI default values for entire CLI tree.

          meta: Return meta data for a specific object, table, or the entire CLI tree.

          schema: Return schema for entire CLI tree.

          '
        enum:
        - default
        - schema
        in: query
        name: action
        required: false
        type: string
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      responses:
        '200':
          description: 'OK: Request returns successful'
          schema:
            properties:
              comment:
                description: Comment.
                maxLength: 255
                type: string
              max-connections:
                description: Maximum number of web proxy WISP connections (4 - 4096,
                  default = 64).
                maximum: 4096
                minimum: 4
                type: integer
              name:
                description: Server name.
                maxLength: 35
                type: string
              outgoing-ip:
                description: WISP outgoing IP address.
                format: ipv4
                type: string
              server-ip:
                description: WISP server IP address.
                format: ipv4
                type: string
              server-port:
                description: WISP server port (1 - 65535, default = 15868).
                maximum: 65535
                minimum: 1
                type: integer
              timeout:
                description: Period of time before WISP requests time out (1 - 15
                  sec, default = 5).
                maximum: 15
                minimum: 1
                type: integer
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Select all entries in a CLI table.\n Access Group: netgrp"
      tags:
      - web-proxy/wisp
    post:
      parameters:
      - description: Possible parameters to go in the body for the request
        in: body
        name: body
        required: true
        schema:
          properties:
            comment:
              description: Comment.
              maxLength: 255
              type: string
            max-connections:
              description: Maximum number of web proxy WISP connections (4 - 4096,
                default = 64).
              maximum: 4096
              minimum: 4
              type: integer
            name:
              description: Server name.
              maxLength: 35
              type: string
            outgoing-ip:
              description: WISP outgoing IP address.
              format: ipv4
              type: string
            server-ip:
              description: WISP server IP address.
              format: ipv4
              type: string
            server-port:
              description: WISP server port (1 - 65535, default = 15868).
              maximum: 65535
              minimum: 1
              type: integer
            timeout:
              description: Period of time before WISP requests time out (1 - 15 sec,
                default = 5).
              maximum: 15
              minimum: 1
              type: integer
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      - description: "If supported, an action can be specified.\n_clone_: Clone this\
          \ specific resource.\nWhen *action=clone* is set, the extra parameters *nkey*\
          \ must be provided.\n__*Note:*__ If this parameter is provided when not\
          \ supported, the action will be ignored and an \u201Cinvalid request\u201D\
          \ error will be returned.\n"
        enum:
        - clone
        in: query
        name: action
        required: false
        type: string
      - description: ' If *action=clone*, use *nkey* to specify the ID for the new
          resource to be created.

          For example, to clone `address1` to `address1_clone`, use:

          __action=clone&nkey=address1_clone__

          __*Note:*__ This parameter can only be used when the *action* parameter
          is set to *clone*.

          '
        in: query
        name: nkey
        required: false
        type: string
      responses:
        '200':
          description: 'OK: Request returns successful'
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Create object(s) in this table.\n Access Group: netgrp"
      tags:
      - web-proxy/wisp
  /web-proxy/wisp/{name}:
    delete:
      parameters:
      - description: mkey
        in: path
        name: name
        required: true
        type: string
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      responses:
        '200':
          description: 'OK: Request returns successful'
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Delete this specific resource.\n Access Group: netgrp"
      tags:
      - web-proxy/wisp
    get:
      parameters:
      - description: Enable to include datasource information for each linked object.
        in: query
        name: datasource
        required: false
        type: boolean
      - description: Enable to include meta information about each object (type id,
          references, etc).
        in: query
        name: with_meta
        required: false
        type: boolean
      - description: Enable to call CLI skip operator to hide skipped properties.
        in: query
        name: skip
        required: false
        type: boolean
      - collectionFormat: pipes
        description: List of property names to include in results, separated by |
          (i.e. policyid|srcintf).
        in: query
        items:
          type: string
        name: format
        required: false
        type: array
      - description: 'default: Return the CLI default values for this object type.

          schema: Return the CLI schema for this object type.

          revision: Return the CMDB revision for this object type.

          transaction-list: List all configuration transaction(s).

          '
        enum:
        - default
        - schema
        - revision
        in: query
        name: action
        required: false
        type: string
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      - description: mkey
        in: path
        name: name
        required: true
        type: string
      responses:
        '200':
          description: 'OK: Request returns successful'
          schema:
            properties:
              comment:
                description: Comment.
                maxLength: 255
                type: string
              max-connections:
                description: Maximum number of web proxy WISP connections (4 - 4096,
                  default = 64).
                maximum: 4096
                minimum: 4
                type: integer
              name:
                description: Server name.
                maxLength: 35
                type: string
              outgoing-ip:
                description: WISP outgoing IP address.
                format: ipv4
                type: string
              server-ip:
                description: WISP server IP address.
                format: ipv4
                type: string
              server-port:
                description: WISP server port (1 - 65535, default = 15868).
                maximum: 65535
                minimum: 1
                type: integer
              timeout:
                description: Period of time before WISP requests time out (1 - 15
                  sec, default = 5).
                maximum: 15
                minimum: 1
                type: integer
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Select a specific entry from a CLI table.\n Access Group: netgrp"
      tags:
      - web-proxy/wisp
    put:
      parameters:
      - description: Possible parameters to go in the body for the request
        in: body
        name: body
        required: true
        schema:
          properties:
            comment:
              description: Comment.
              maxLength: 255
              type: string
            max-connections:
              description: Maximum number of web proxy WISP connections (4 - 4096,
                default = 64).
              maximum: 4096
              minimum: 4
              type: integer
            name:
              description: Server name.
              maxLength: 35
              type: string
            outgoing-ip:
              description: WISP outgoing IP address.
              format: ipv4
              type: string
            server-ip:
              description: WISP server IP address.
              format: ipv4
              type: string
            server-port:
              description: WISP server port (1 - 65535, default = 15868).
              maximum: 65535
              minimum: 1
              type: integer
            timeout:
              description: Period of time before WISP requests time out (1 - 15 sec,
                default = 5).
              maximum: 15
              minimum: 1
              type: integer
      - collectionFormat: csv
        description: 'Specify the Virtual Domain(s) from which results are returned
          or changes are applied to. If this parameter is not provided, the management
          VDOM will be used. If the admin does not have access to the VDOM, a permission
          error will be returned.

          The URL parameter is one of:

          vdom=root (Single VDOM)

          vdom=vdom1,vdom2 (Multiple VDOMs)

          vdom=* (All VDOMs)

          '
        in: query
        items:
          type: string
        name: vdom
        required: false
        type: array
      - description: "If supported, an action can be specified.\n_move_: Move this\
          \ specific resource.\nWhen *action=move* is set, one of the extra parameters\
          \ (*before*, *after*) must be provided.\n__*Note:*__ If this parameter is\
          \ provided when not supported, the action will be ignored and an \u201C\
          invalid request\u201D error will be returned.\n"
        enum:
        - move
        in: query
        name: action
        required: false
        type: string
      - description: 'If *action=move*, use *before* to specify the ID of the resource
          that this resource will be moved before.

          For example, to move `object 1` to before `object 2`, use:

          __action=move&before=2__

          __*Note:*__ This parameter can only be used when the *action* parameter
          is set to *move*.

          '
        in: query
        name: before
        required: false
        type: string
      - description: 'If *action=move*, use *after* to specify the ID of the resource
          that this resource will be moved after.

          For example, to move `object 1` to after `object 3`, use:

          __action=move&after=3__

          __*Note:*__ This parameter can only be used when the *action* parameter
          is set to *move*.

          '
        in: query
        name: after
        required: false
        type: string
      - description: mkey
        in: path
        name: name
        required: true
        type: string
      responses:
        '200':
          description: 'OK: Request returns successful'
        '400':
          description: 'Bad Request: Request cannot be processed by the API'
        '401':
          description: 'Not Authorized: Request without successful login session'
        '403':
          description: 'Forbidden: Request is missing CSRF token or administrator
            is missing access profile permissions.'
        '404':
          description: 'Resource Not Found: Unable to find the specified resource.'
        '405':
          description: 'Method Not Allowed: Specified HTTP method is not allowed for
            this resource. '
        '413':
          description: 'Request Entity Too Large: Request cannot be processed due
            to large entity '
        '424':
          description: 'Failed Dependency: Fail dependency can be duplicate resource,
            missing required parameter, missing required attribute, invalid attribute
            value'
        '429':
          description: 'Access temporarily blocked: Maximum failed authentications
            reached. The offended source is temporarily blocked for certain amount
            of time.'
        '500':
          description: 'Internal Server Error: Internal error when processing the
            request '
      summary: "Update this specific resource.\n Access Group: netgrp"
      tags:
      - web-proxy/wisp
produces:
- application/json
schemes:
- https
security:
- APIKeyQueryParam: []
securityDefinitions:
  APIKeyQueryParam:
    in: query
    name: access_token
    type: apiKey
swagger: '2.0'
tags:
- description: Configure debug URL addresses.
  name: web-proxy/debug-url
- description: Configure explicit Web proxy settings.
  name: web-proxy/explicit
- description: Configure forward-server addresses.
  name: web-proxy/forward-server
- description: Configure a forward server group consisting or multiple forward servers.
    Supports failover and load balancing.
  name: web-proxy/forward-server-group
- description: Configure Web proxy global settings.
  name: web-proxy/global
- description: Configure web proxy profiles.
  name: web-proxy/profile
- description: Exempt URLs from web proxy forwarding and caching.
  name: web-proxy/url-match
- description: Configure Wireless Internet service provider (WISP) servers.
  name: web-proxy/wisp
